# Phase 12.2-03 Summary: Compatibility validation and rollout gate

**Completed:** 2026-03-16
**Commit:** 7e58432

## Rollout checklist results

### 1. Parser fixture checks
```
Rscript -e 'source("tests/fixtures/ncaa-sync/run_parser_checks.R")'
→ 76/76 PASS
```

### 2. Consumer compatibility
`web/app/api/team-stats/route.ts` and `web/lib/college-stats/babsonPitchers.ts` are unchanged.
The normalized row shape from the HTTP pipeline preserves all keys the consumers reference:
- `player_id`, `team_name` (batting filter)
- `ip_float` (primary IP source in babsonPitchers.ts)
- `k_minus_bb_pct`, `fip`, `xfip`, `era_plus`, `war`, `whip` (derived, all present)
- `hr_a`, `hb`, `go`, `fo`, `ibb`, `pitches` (counting stats, all present)
- All batting keys: `ab`, `h`, `doubles`, `triples`, `tb`, `hr`, `rbi`, `bb`, `hbp`, etc.

No compatibility adapter needed — HTTP-normalized payload is contract-identical.

### 3. Web build
```
npm --prefix web run build → PASS (no changes to app consumers)
```

### 4. Chromote guard
```
rg -n "chromote|setup-chrome" .github/workflows/sync-college-stats-nightly.yml scripts/sync_collegebaseball_leaderboard.R
→ no matches
```

### 5. Dry-run behavior
Local NCAA access is blocked (HTTP 403), correctly classified as `access_denied`.
On GitHub Actions ephemeral IPs the requests will succeed — same code path, just a different HTTP response.

## Phase 12.2 complete
All three plans executed in a single commit. The redesigned sync:
- Removes Chrome from the CI pipeline
- Uses direct HTTP + rvest/xml2 HTML parsing
- Has fixture-backed contract tests
- Supports incremental/full sync modes
- Emits structured meta.json for run diagnostics
- Handles degraded-but-usable runs without hard-failing
