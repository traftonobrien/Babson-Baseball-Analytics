---
phase: 12.2-ncaa-leaderboard-sync-redesign
plan: "03"
type: execute
wave: 3
depends_on: ["12.2-01", "12.2-02"]
files_modified:
  - scripts/sync_collegebaseball_leaderboard.R
  - web/app/api/team-stats/route.ts
  - web/lib/college-stats/babsonPitchers.ts
  - web/lib/collegeStats.ts
  - tests/fixtures/ncaa-sync/
autonomous: true
requirements: [SC3, SC4, SC5, SC6]

must_haves:
  truths:
    - "The redesigned sync is proven compatible with the current team-stats API and Babson leaderboard helpers before rollout."
    - "Validation checks explicitly cover Babson presence, row-shape compatibility, and degraded-run handling rather than assuming output correctness."
    - "Any required compatibility adapters are shipped in the same phase if the HTTP-normalized payload cannot exactly match the current contract."
    - "The phase closes with a reproducible verification gate for both local dry runs and CI execution."
  artifacts:
    - path: "tests/fixtures/ncaa-sync/"
      provides: "Compatibility snapshots and parser regression checks against expected batting/pitching row shapes"
    - path: "tests/fixtures/ncaa-sync/run_parser_checks.R"
      provides: "Executable fixture/contract runner used by the rollout checklist"
    - path: "web/app/api/team-stats/route.ts"
      provides: "Confirmed or minimally adapted consumer of the preserved JSON contract"
    - path: "web/lib/college-stats/babsonPitchers.ts"
      provides: "Confirmed or minimally adapted Babson pitching compatibility layer and derived-metric assumptions"
    - path: "web/lib/collegeStats.ts"
      provides: "Final fetch path validation for the published leaderboard payloads"
  key_links:
    - from: "web/app/api/team-stats/route.ts"
      to: "web/lib/collegeStats.ts"
      via: "leaderboard JSON loading path exercised after sync changes"
      pattern: "fetchPitchingLeaderboard"
    - from: "web/lib/college-stats/babsonPitchers.ts"
      to: "web/public/college-stats/pitching-2026.json"
      via: "derived metrics built from preserved pitcher row fields"
      pattern: "k_minus_bb_pct"
---

<objective>
Close the phase with compatibility proof and rollout-safe validation.

Purpose: ensure the fetch redesign remains invisible to the app except for better reliability and diagnostics, and catch any contract drift before the workflow becomes the new default sync path.
</objective>

<context>
@.planning/phases/12.2-ncaa-leaderboard-sync-redesign/12.2-CONTEXT.md
@.planning/phases/12.2-ncaa-leaderboard-sync-redesign/12.2-PRD.md
@scripts/sync_collegebaseball_leaderboard.R
@web/app/api/team-stats/route.ts
@web/lib/college-stats/babsonPitchers.ts
@web/lib/collegeStats.ts
</context>

<tasks>
<task type="code" tdd="true">
  <name>Add contract and compatibility checks for published JSON</name>
  <files>tests/fixtures/ncaa-sync/, scripts/sync_collegebaseball_leaderboard.R</files>
  <action>Create validation helpers or fixture-based checks that assert the emitted batting and pitching payloads still satisfy the current consumer contract, including required keys, numeric coercion expectations, and Babson row presence when source data exists.</action>
  <verify>Compatibility checks fail loudly if required fields such as `player_id`, `team_name`, `ip_float`, or `k_minus_bb_pct` disappear or change type unexpectedly.</verify>
  <done>The phase has an explicit contract gate rather than relying on manual inspection of JSON files.</done>
</task>

<task type="code" tdd="false">
  <name>Harden consumer-facing compatibility seams only if needed</name>
  <files>web/app/api/team-stats/route.ts, web/lib/college-stats/babsonPitchers.ts, web/lib/collegeStats.ts</files>
  <action>Review the current API and leaderboard helpers against the redesigned payloads and add the smallest necessary compatibility adapter in the same phase if the new source cannot preserve every current field exactly, while avoiding UI-level changes.</action>
  <verify>The team-stats API and Babson filtering logic continue to function against regenerated NCAA JSON without requiring leaderboard page rewrites.</verify>
  <done>Any unavoidable contract seam is handled once, centrally, and shipped together with the sync redesign.</done>
</task>

<task type="code" tdd="false">
  <name>Run the final validation and rollout gate</name>
  <files>scripts/sync_collegebaseball_leaderboard.R, tests/fixtures/ncaa-sync/</files>
  <action>Define and execute the full verification sequence for dry-run syncs, metadata inspection, consumer compatibility checks, and CI search guards so Phase 12.2 can ship with a clear go/no-go checklist.</action>
  <verify>All validation commands pass and the repo has a reproducible checklist for future NCAA parser regressions.</verify>
  <done>The redesigned sync is ready to replace the old pipeline in scheduled use.</done>
</task>
</tasks>

<verification>
1. `Rscript scripts/sync_collegebaseball_leaderboard.R --years 2026 --types pitching batting --team-name Babson --sync-mode incremental --dry-run`
2. `npm --prefix web test -- --run lib/college-stats`
3. `npm --prefix web run build`
4. `npm --prefix web run dev`
5. `jq '.pitchers | length, .qualified' <(curl -s http://localhost:3000/api/team-stats?statType=pitching)`
6. `jq '.hitters | length' <(curl -s http://localhost:3000/api/team-stats?statType=batting)`
7. `Rscript -e 'source("tests/fixtures/ncaa-sync/run_parser_checks.R")'`
8. `rg -n "collegebaseball\\+chromote|chromote|setup-chrome" web/public/college-stats/meta.json .github/workflows/sync-college-stats-nightly.yml scripts/sync_collegebaseball_leaderboard.R`
</verification>

<success_criteria>
- Published NCAA JSON is contract-compatible with current app consumers
- Babson validation and row-shape checks are automated
- Any required compatibility adapter is included in the same phase
- Final dry-run, test, build, and workflow guard checks define a clear rollout gate
</success_criteria>

<output>
After completion, create `.planning/phases/12.2-ncaa-leaderboard-sync-redesign/12.2-03-SUMMARY.md`
</output>
