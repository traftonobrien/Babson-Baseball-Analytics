---
phase: 12.2-ncaa-leaderboard-sync-redesign
plan: "02"
type: execute
wave: 2
depends_on: ["12.2-01"]
files_modified:
  - scripts/sync_collegebaseball_leaderboard.R
  - .github/workflows/sync-college-stats-nightly.yml
  - web/public/college-stats/meta.json
autonomous: true
requirements: [SC1, SC2, SC4, SC5]

must_haves:
  truths:
    - "The sync supports both nightly incremental mode and manual or scheduled full rebuild mode from one script contract."
    - "Structured failure classification is aggregated into `meta.json` so empty early-season teams are distinguishable from access or parser regressions."
    - "Nightly workflow behavior warns on degraded-but-usable runs and fails only on real pipeline breakage."
    - "The GitHub Actions workflow no longer installs Chrome or Chromote for NCAA sync."
    - "Incremental team selection is explicit and reproducible instead of left as an open heuristic."
    - "Workflow control flow is refactored so publish and health evaluation can still run after degraded sync legs."
    - "Degraded publish semantics are explicit: healthy legs publish, degraded legs keep their prior artifact unchanged, and mixed-generation partial overwrites are not allowed."
  artifacts:
    - path: "scripts/sync_collegebaseball_leaderboard.R"
      provides: "Scope-aware CLI for incremental vs full sync plus metadata emission"
    - path: ".github/workflows/sync-college-stats-nightly.yml"
      provides: "HTTP-first nightly workflow with manual full rebuild path and warning/failure policy"
    - path: "web/public/college-stats/meta.json"
      provides: "Structured freshness and failure diagnostics for batting and pitching sync runs"
  key_links:
    - from: ".github/workflows/sync-college-stats-nightly.yml"
      to: "scripts/sync_collegebaseball_leaderboard.R"
      via: "mode and validation flags passed from schedule or workflow_dispatch"
      pattern: "--sync-mode"
    - from: "web/public/college-stats/meta.json"
      to: "scripts/sync_collegebaseball_leaderboard.R"
      via: "aggregated result counters and run diagnostics emitted by the script"
      pattern: "empty_stats"
---

<objective>
Redesign the operational behavior of the NCAA sync around sync scope, metadata, and failure policy.

Purpose: turn the new HTTP fetch architecture into a maintainable nightly job by reducing unnecessary full sweeps, classifying degraded outcomes correctly, and making run health visible in metadata and workflow logs.
</objective>

<context>
@.planning/phases/12.2-ncaa-leaderboard-sync-redesign/12.2-CONTEXT.md
@.planning/phases/12.2-ncaa-leaderboard-sync-redesign/12.2-PRD.md
@.github/workflows/sync-college-stats-nightly.yml
@scripts/sync_collegebaseball_leaderboard.R
@web/public/college-stats/meta.json
</context>

<tasks>
<task type="code" tdd="false">
  <name>Add incremental and full sync modes to the script contract</name>
  <files>scripts/sync_collegebaseball_leaderboard.R</files>
  <action>Introduce an explicit CLI mode such as `--sync-mode incremental|full`, preserve a deterministic full rebuild path, and make the chosen scope visible in logs and metadata. Define nightly incremental selection concretely: always include Babson; union in every `team_id` present in the latest published batting/pitching JSON and prior failure metadata; allow an optional watch-list input; and rely on a scheduled weekly full rebuild to discover newly active teams not yet seen in cache.</action>
  <verify>Incremental mode can run a constrained team set without changing output file names, while full mode still covers the full Division III universe for manual rebuilds.</verify>
  <done>The sync matches actual operational needs instead of forcing a nightly full-division sweep.</done>
</task>

<task type="code" tdd="true">
  <name>Publish structured failure and freshness metadata</name>
  <files>scripts/sync_collegebaseball_leaderboard.R, web/public/college-stats/meta.json</files>
  <action>Replace the current metadata rebuild step with script-owned structured diagnostics that include mode, source, synced timestamp, row counts, team counts, Babson validation status, and categorized totals for `success`, `empty_stats`, `access_denied`, and `parse_failed`, plus representative failure details for debugging.</action>
  <verify>`meta.json` can distinguish an early-season mostly-empty run from a parser regression or NCAA denial event without reading raw workflow logs.</verify>
  <done>Maintainers have one machine-readable place to understand run health and freshness.</done>
</task>

<task type="code" tdd="false">
  <name>Refit the GitHub Actions workflow around the new runtime policy</name>
  <files>.github/workflows/sync-college-stats-nightly.yml</files>
  <action>Remove Chrome setup and Chromote installation, wire the workflow to the new sync-mode flags, keep manual full rebuild support through `workflow_dispatch`, and restructure job control so each sync leg always uploads its artifact and health result, `publish` runs with `if: always()`, and a final evaluation step decides success vs warning vs failure after reading metadata and Babson validation status. Replace the current matrix-blocking behavior with explicit artifact-based health evaluation so degraded runs can still publish and warn. Lock mixed-generation behavior: if only one leg is healthy, publish the healthy artifact, keep the prior published JSON for the degraded leg unchanged, mark that leg stale/degraded in `meta.json`, and avoid overwriting a failed leg with partial output.</action>
  <verify>The workflow definition clearly separates nightly incremental behavior from full rebuild behavior and no longer installs or references Chrome.</verify>
  <done>The CI path matches the new architecture and no longer treats early-season empties as automatic hard failures.</done>
</task>
</tasks>

<verification>
1. `Rscript scripts/sync_collegebaseball_leaderboard.R --years 2026 --types pitching batting --sync-mode incremental --dry-run`
2. `Rscript scripts/sync_collegebaseball_leaderboard.R --years 2026 --types pitching batting --sync-mode full --limit 10 --dry-run`
3. `jq '.results' web/public/college-stats/meta.json`
4. `jq '.results["2026-pitching"]' web/public/college-stats/meta.json`
5. `rg -n "chrome|chromote" .github/workflows/sync-college-stats-nightly.yml scripts/sync_collegebaseball_leaderboard.R`
6. `rg -n "if: always|workflow_dispatch|sync-mode|warning" .github/workflows/sync-college-stats-nightly.yml`
</verification>

<success_criteria>
- Nightly incremental and manual full rebuild modes are both supported
- `meta.json` reports structured outcome classification and freshness diagnostics
- Degraded-but-usable runs warn instead of hard-failing when Babson and publish validation pass
- The workflow no longer installs or references Chrome/Chromote
</success_criteria>

<output>
After completion, create `.planning/phases/12.2-ncaa-leaderboard-sync-redesign/12.2-02-SUMMARY.md`
</output>
