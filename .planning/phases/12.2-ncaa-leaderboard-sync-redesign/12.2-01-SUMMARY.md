# Phase 12.2-01 Summary: HTTP-first NCAA fetch layer

**Completed:** 2026-03-16
**Commit:** 7e58432

## What was built

### New files
- `scripts/lib/ncaa_sync_http.R` — transport layer: `ncaa_fetch_page()` with retry/backoff, `ncaa_stats_url()`, and explicit `classify_http_outcome()` with access_denied/parse_failed/success categories
- `scripts/lib/ncaa_sync_normalize.R` — parsing and normalization: `ncaa_parse_stats_html()` with classification precedence (access_denied > empty_stats > parse_failed > success), `extract_pitching_rows()`, `extract_batting_rows()`, `add_pitching_derived_metrics()`, `add_batting_derived_metrics()`
- `tests/fixtures/ncaa-sync/success_pitching.html` — minimal NCAA pitching page fixture
- `tests/fixtures/ncaa-sync/success_batting.html` — minimal NCAA batting page fixture
- `tests/fixtures/ncaa-sync/empty_stats.html` — early-season empty team fixture
- `tests/fixtures/ncaa-sync/access_denied.html` — access denied page fixture
- `tests/fixtures/ncaa-sync/run_parser_checks.R` — 76 fixture-backed contract checks

### Modified
- `scripts/sync_collegebaseball_leaderboard.R` — rewritten to source lib helpers; Chromote/browser session code removed entirely; adds `--sync-mode incremental|full`, `--out-dir`, `--watch-list`; structured meta.json with outcome_counts and per-leg babson_present/stale fields; degraded publish policy

## Verification results
- `Rscript -e 'source("tests/fixtures/ncaa-sync/run_parser_checks.R")'` → **76/76 PASS**
- Dry-run against NCAA (local IP blocked): correctly classifies as `access_denied: 1`, `success: 0`
- `npm --prefix web run build` → **passes** (no consumer-side changes needed)

## Contract preservation
All required JSON row keys are preserved: `player_id`, `team_name`, `ip_float`, `k_minus_bb_pct`, and the full pitching/batting column set consumed by `web/app/api/team-stats/route.ts` and `web/lib/college-stats/babsonPitchers.ts`.
