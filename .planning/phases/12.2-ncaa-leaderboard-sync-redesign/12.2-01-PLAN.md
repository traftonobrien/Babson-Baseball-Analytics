---
phase: 12.2-ncaa-leaderboard-sync-redesign
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/sync_collegebaseball_leaderboard.R
  - scripts/lib/ncaa_sync_http.R
  - scripts/lib/ncaa_sync_normalize.R
  - tests/fixtures/ncaa-sync/
autonomous: true
requirements: [SC1, SC3, SC4]

must_haves:
  truths:
    - "The NCAA sync replaces Chromote and Chrome session management with direct HTTP fetch plus HTML parsing."
    - "Fetch, parse, normalize, and publish responsibilities are split so parser failures can be diagnosed without reading one monolithic script."
    - "The published batting and pitching row shapes remain compatible with the existing app JSON contract."
    - "Per-team fetch results carry structured status information that later plans can aggregate into metadata and workflow decisions."
    - "Fetch outcome classification has explicit precedence rules so empty-team pages, access denials, and parser regressions do not collapse into one generic error bucket."
  artifacts:
    - path: "scripts/sync_collegebaseball_leaderboard.R"
      provides: "Main CLI orchestrator for the redesigned HTTP-first sync pipeline"
    - path: "scripts/lib/ncaa_sync_http.R"
      provides: "Direct NCAA request helpers, retry/backoff policy, and raw HTML fetch classification"
    - path: "scripts/lib/ncaa_sync_normalize.R"
      provides: "Team-page parsing, player-link extraction, and contract-preserving row normalization"
    - path: "tests/fixtures/ncaa-sync/"
      provides: "Saved NCAA HTML fixtures for batting/pitching success, empty, denied, and parser-drift cases"
    - path: "tests/fixtures/ncaa-sync/run_parser_checks.R"
      provides: "Fixture runner that asserts classification and contract-preserving normalization behavior"
  key_links:
    - from: "scripts/lib/ncaa_sync_normalize.R"
      to: "web/app/api/team-stats/route.ts"
      via: "published JSON row keys consumed by the team-stats API"
      pattern: "player_id"
    - from: "scripts/lib/ncaa_sync_normalize.R"
      to: "web/lib/college-stats/babsonPitchers.ts"
      via: "pitching leaderboard contract used for Babson filtering and derived metrics"
      pattern: "ip_float"
---

<objective>
Replace the fragile browser-driven NCAA fetch path with a direct HTTP architecture while preserving the current JSON contract.

Purpose: solve the highest-risk implementation problem first so later workflow and validation work can build on a stable fetch/normalize boundary instead of the current Chromote monolith.
</objective>

<context>
@.planning/phases/12.2-ncaa-leaderboard-sync-redesign/12.2-CONTEXT.md
@.planning/phases/12.2-ncaa-leaderboard-sync-redesign/12.2-PRD.md
@.github/workflows/sync-college-stats-nightly.yml
@scripts/sync_collegebaseball_leaderboard.R
@web/app/api/team-stats/route.ts
@web/lib/college-stats/babsonPitchers.ts
</context>

<tasks>
<task type="code" tdd="true">
  <name>Build the HTTP-first NCAA fetch layer</name>
  <files>scripts/sync_collegebaseball_leaderboard.R, scripts/lib/ncaa_sync_http.R</files>
  <action>Remove Chromote session setup and implement direct HTTP request helpers for NCAA stat pages, including user-agent headers, retry/backoff, timeout handling, and first-pass classification of fetch outcomes such as `success`, `empty_stats`, `access_denied`, and `parse_failed`.</action>
  <verify>The script can request batting and pitching stat pages without Chrome and returns structured fetch results instead of raw `tryCatch` errors.</verify>
  <done>The sync no longer depends on a browser runtime and has a durable transport boundary for later workflow policy.</done>
</task>

<task type="code" tdd="true">
  <name>Split parsing and normalization away from transport with explicit status rules</name>
  <files>scripts/sync_collegebaseball_leaderboard.R, scripts/lib/ncaa_sync_normalize.R, tests/fixtures/ncaa-sync/, tests/fixtures/ncaa-sync/run_parser_checks.R</files>
  <action>Extract HTML parsing, `#stat_grid` link resolution, batting row normalization, and pitching row normalization into explicit helpers that can operate on saved fixture HTML so parser drift can be tested independently from live NCAA availability. Define classification precedence explicitly: `access_denied` when the response title/body contains NCAA denial markers; `empty_stats` when the page is reachable and the stats table renders but contains no player rows after removing blanks/subheaders; `parse_failed` when the page is reachable but the expected stat table or required columns/links are missing after excluding the empty-page case; `success` when normalization yields valid rows.</action>
  <verify>Fixture-backed parser checks prove the normalizers still emit the current batting and pitching row keys expected by the app consumers, including `player_id`, `team_name`, and `ip_float` for pitchers.</verify>
  <done>The pipeline has isolated parser/contract logic rather than mixing DOM access, transformation, and publishing in one function.</done>
</task>

<task type="code" tdd="false">
  <name>Preserve the current publish contract</name>
  <files>scripts/sync_collegebaseball_leaderboard.R, scripts/lib/ncaa_sync_normalize.R</files>
  <action>Keep the output writer producing `web/public/college-stats/batting-2026.json` and `web/public/college-stats/pitching-2026.json` with the same row-level shape currently consumed by `web/app/api/team-stats/route.ts` and `web/lib/college-stats/babsonPitchers.ts`, reusing existing derived metric inputs instead of redefining them.</action>
  <verify>Dry-run output diffs against the current contract show additive metadata only where explicitly allowed and no breaking row-key changes.</verify>
  <done>The fetch redesign stays implementation-local and does not force app-side leaderboard rewrites.</done>
</task>
</tasks>

<verification>
1. `Rscript scripts/sync_collegebaseball_leaderboard.R --years 2026 --types pitching --team-name Babson --dry-run`
2. `Rscript scripts/sync_collegebaseball_leaderboard.R --years 2026 --types batting --team-name Babson --dry-run`
3. `Rscript -e 'source("scripts/lib/ncaa_sync_normalize.R"); source("tests/fixtures/ncaa-sync/run_parser_checks.R")'`
4. `Rscript scripts/sync_collegebaseball_leaderboard.R --years 2026 --types pitching batting --team-name Babson --out-dir /tmp/ncaa-sync-check --write-output`
5. `jq '.[0] | keys' /tmp/ncaa-sync-check/pitching-2026.json`
6. `jq '.[0] | keys' /tmp/ncaa-sync-check/batting-2026.json`
</verification>

<success_criteria>
- Chrome and Chromote are removed from the sync implementation path
- Fetch, parse, normalize, and publish responsibilities are isolated
- Direct HTTP requests produce classified per-team outcomes
- Batting and pitching JSON rows preserve the current app-facing contract
</success_criteria>

<output>
After completion, create `.planning/phases/12.2-ncaa-leaderboard-sync-redesign/12.2-01-SUMMARY.md`
</output>
