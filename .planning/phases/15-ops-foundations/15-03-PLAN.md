---
phase: 15-ops-foundations
plan: 03
type: execute
wave: 2
depends_on:
  - 15-01
autonomous: false
files_modified:
  - .planning/phases/15-ops-foundations/ENV-AUDIT.md
requirements:
  - OPS-03

must_haves:
  truths:
    - "PT_PASSWORD, MECHANICS_PASSWORD, and DATABASE_URL are confirmed present in Vercel production environment"
    - "A protected page route returns a redirect to /login when hit without cookies — middleware is live"
    - "The charting API responds correctly — DATABASE_URL is valid and the DB connection works"
    - "An audit document records what was checked and the outcome"
  artifacts:
    - path: ".planning/phases/15-ops-foundations/ENV-AUDIT.md"
      provides: "Audit record of Vercel env var confirmation and middleware verification"
  key_links:
    - from: "Vercel dashboard env vars"
      to: "web/lib/auth.ts"
      via: "PT_PASSWORD → AUTH_GATES.site, MECHANICS_PASSWORD → AUTH_GATES.mechanics"
      pattern: "process.env.PT_PASSWORD"
---

<objective>
Confirm that all three required environment variables are present and correct on the live Vercel deployment, and that the middleware deployed in Plan 15-01 is actually protecting routes.

Purpose: Even with middleware.ts deployed, if env vars are missing or wrong the app silently fails or lets users through. This plan verifies the full end-to-end auth chain is live.

Output: Human-verified confirmation + ENV-AUDIT.md record.
</objective>

<execution_context>
@/Users/traftonobrien/.claude/get-shit-done/workflows/execute-plan.md
@/Users/traftonobrien/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@web/lib/auth.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Prepare verification checklist document</name>
  <files>.planning/phases/15-ops-foundations/ENV-AUDIT.md</files>
  <action>
Create .planning/phases/15-ops-foundations/ENV-AUDIT.md with a structured checklist the human will fill out during the checkpoint. Write the file now so the human has a record to update.

Content:

```markdown
# Phase 15 — Env Var and Middleware Audit

**Date:** [fill in]
**Vercel Project URL:** [fill in]

## Environment Variables

Checked in: Vercel Dashboard → Project → Settings → Environment Variables

| Variable | Present | Value looks correct | Notes |
|----------|---------|---------------------|-------|
| PT_PASSWORD | [ ] yes / [ ] no | [ ] yes / [ ] no | |
| MECHANICS_PASSWORD | [ ] yes / [ ] no | [ ] yes / [ ] no | |
| DATABASE_URL | [ ] yes / [ ] no | [ ] yes / [ ] no | Should start with postgres:// or postgresql:// |

## Middleware Verification (after Plan 15-01 deploys)

Tested at: [Vercel deployment URL]

| Test | Expected | Actual | Pass? |
|------|----------|--------|-------|
| GET /charting (no cookie) | Redirect to /login | | |
| GET /mechanics (no cookie) | Redirect to /mechanics-login | | |
| GET /command (no cookie) | Redirect to /login | | |
| GET /login (no cookie) | 200 OK (public) | | |
| GET /api/charting/ping (with valid pt_auth cookie) | 200 OK | | |

## Database Connectivity

- [ ] GET /api/charting/games returns 200 (or 401 if unauthenticated) — not 500
- [ ] No DATABASE_URL errors visible in Vercel function logs

## Overall Result

- [ ] All env vars confirmed present and correct
- [ ] Middleware redirects working on live deployment
- [ ] Database connectivity confirmed

**Signed off by:** [initials]
```
  </action>
  <verify>
    <automated>ls /Users/traftonobrien/Desktop/pitch-tracker/.planning/phases/15-ops-foundations/ENV-AUDIT.md && echo "PASS"</automated>
  </verify>
  <done>ENV-AUDIT.md checklist file exists in phase directory</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
Plan 15-01 deployed middleware.ts to Vercel. This checkpoint verifies:
1. The three required env vars (PT_PASSWORD, MECHANICS_PASSWORD, DATABASE_URL) are present in Vercel
2. The deployed middleware is actually redirecting unauthenticated requests

Claude has created .planning/phases/15-ops-foundations/ENV-AUDIT.md with a structured checklist.
  </what-built>
  <how-to-verify>
Step 1 — Check Vercel environment variables:
- Open Vercel Dashboard → your pitch-tracker project → Settings → Environment Variables
- Confirm PT_PASSWORD, MECHANICS_PASSWORD, and DATABASE_URL are all present under "Production"
- PT_PASSWORD and MECHANICS_PASSWORD should be non-empty strings
- DATABASE_URL should start with postgres:// or postgresql://

Step 2 — Verify middleware redirects (open an incognito/private window with no cookies):
- Visit [your-vercel-url]/charting → should redirect to /login
- Visit [your-vercel-url]/mechanics → should redirect to /mechanics-login
- Visit [your-vercel-url]/login → should load the login page (200)
- Visit [your-vercel-url]/command → should redirect to /login

Step 3 — Verify database:
- Log into the site (use PT_PASSWORD from Vercel)
- Visit /charting → should load the charting hub (not a 500 error)

Step 4 — Fill in .planning/phases/15-ops-foundations/ENV-AUDIT.md with results

If any env var is missing: add it in Vercel Dashboard → Environment Variables → Add New, then redeploy.
If middleware is not redirecting: check that web/middleware.ts deployed (Vercel → Deployments → latest → Functions).
  </how-to-verify>
  <resume-signal>Type "verified" if all checks pass, or describe any issues found</resume-signal>
</task>

</tasks>

<verification>
ENV-AUDIT.md exists and is filled out. Human has confirmed:
- All three env vars present in Vercel production
- Middleware redirects working on live deployment (unauthenticated requests hit /login)
- Database connectivity confirmed (charting hub loads without 500 errors)
</verification>

<success_criteria>
1. ENV-AUDIT.md filled out with concrete yes/no results for all checks
2. Human confirmed: PT_PASSWORD, MECHANICS_PASSWORD, DATABASE_URL all present in Vercel
3. Human confirmed: unauthenticated /charting redirects to /login on live deployment
4. Human confirmed: charting hub loads without 500 errors (DB connectivity OK)
</success_criteria>

<output>
After completion, create `.planning/phases/15-ops-foundations/15-03-SUMMARY.md`
</output>
