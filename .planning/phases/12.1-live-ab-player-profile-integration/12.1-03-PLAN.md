---
phase: 12.1-live-ab-player-profile-integration
plan: "03"
type: execute
wave: 3
depends_on: ["12.1-01", "12.1-02"]
files_modified:
  - web/lib/charting/playerProfile.ts
  - web/lib/charting/playerProfile.test.ts
  - web/app/players/[slug]/PlayerProfileTabs.tsx
  - web/app/players/[slug]/LiveAbProfilePanel.tsx
autonomous: true
requirements: [PORT-05, SC1, SC2, SC3, SC4]

must_haves:
  truths:
    - "Two-way players, hitter-only players, and no-data players all get clear, non-misleading Live AB states."
    - "Recent-session links from the Live AB tab route into the intended charting destinations."
    - "The profile route remains build-clean and regression-safe after Live AB integration."
    - "The final Phase 12.1 experience feels coherent inside the existing player profile system."
  artifacts:
    - path: "web/lib/charting/playerProfile.test.ts"
      provides: "Regression coverage for mixed-role and empty-state behavior"
    - path: "web/app/players/[slug]/LiveAbProfilePanel.tsx"
      provides: "Polished final Live AB profile presentation"
    - path: "web/app/players/[slug]/PlayerProfileTabs.tsx"
      provides: "Finalized tab behavior and route wiring"
  key_links:
    - from: "web/app/players/[slug]/LiveAbProfilePanel.tsx"
      to: "web/app/charting/games/[id]/page.tsx"
      via: "recent-session navigation"
      pattern: "/charting/games/"
---

<objective>
Polish the Live AB profile integration and close the verification gate.

Purpose: finish the role-specific UX details, prove the navigation/empty-state behavior, and leave the player profile route stable enough to ship.
</objective>

<context>
@.planning/phases/12.1-live-ab-player-profile-integration/12.1-CONTEXT.md
@.planning/phases/12.1-live-ab-player-profile-integration/12.1-RESEARCH.md
@.planning/phases/12.1-live-ab-player-profile-integration/12.1-VALIDATION.md
@web/lib/charting/playerProfile.ts
@web/app/players/[slug]/PlayerProfileTabs.tsx
@web/app/players/[slug]/LiveAbProfilePanel.tsx
</context>

<tasks>
<task type="code" tdd="true">
  <name>Finalize mixed-role, empty-state, and link behavior</name>
  <files>web/lib/charting/playerProfile.ts, web/lib/charting/playerProfile.test.ts, web/app/players/[slug]/LiveAbProfilePanel.tsx</files>
  <action>Refine the helper and panel so two-way players, hitter-only players, and no-data players each get clear role-aware messaging, and ensure recent-session links and any leaderboard drill-outs are consistent.</action>
  <verify>Tests cover mixed-role/no-data behavior and route targets are deterministic.</verify>
  <done>The Live AB tab behaves reliably across the full player-profile matrix, not just the happy-path pitcher case.</done>
</task>

<task type="code" tdd="false">
  <name>Run final route polish and validation gates</name>
  <files>web/app/players/[slug]/PlayerProfileTabs.tsx, web/app/players/[slug]/LiveAbProfilePanel.tsx</files>
  <action>Polish spacing, copy, and tab behavior as needed after integration, then run the full phase validation commands to confirm the profile route remains stable.</action>
  <verify>`npm --prefix web test -- --run` and `npm --prefix web run build` both pass.</verify>
  <done>The Phase 12.1 profile experience is visually coherent and technically ready for verification.</done>
</task>
</tasks>

<verification>
1. `npm --prefix web test -- --run lib/charting/playerProfile`
2. `npm --prefix web test -- --run`
3. `npm --prefix web run build`
</verification>

<success_criteria>
- Two-way and empty-state behavior are explicit and tested
- Recent-session and drill-out navigation is stable
- Full test suite and build remain green
- Live AB feels like a first-class player profile tab
</success_criteria>

<output>
After completion, create `.planning/phases/12.1-live-ab-player-profile-integration/12.1-03-SUMMARY.md`
</output>
