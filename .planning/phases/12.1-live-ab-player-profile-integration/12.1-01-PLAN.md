---
phase: 12.1-live-ab-player-profile-integration
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - web/lib/charting/playerProfile.ts
  - web/lib/charting/playerProfile.test.ts
autonomous: true
requirements: [PORT-05, SC2, SC3, SC4]

must_haves:
  truths:
    - "One charting profile helper resolves a player slug into pitcher Live AB data, hitter Live AB data, or both."
    - "The helper reuses shared charting analytics and canonical player identity utilities instead of introducing duplicate formulas."
    - "Hitter matching and no-data behavior are explicit enough that later UI work can trust the returned role availability."
    - "Recent charting sessions are returned in a display-ready shape for profile consumption."
  artifacts:
    - path: "web/lib/charting/playerProfile.ts"
      provides: "Role-aware Live AB profile aggregation and identity resolution"
    - path: "web/lib/charting/playerProfile.test.ts"
      provides: "Unit coverage for pitcher-only, hitter-only, two-way, and no-data profile cases"
  key_links:
    - from: "web/lib/charting/playerProfile.ts"
      to: "web/lib/charting/analytics.ts"
      via: "aggregatePitcherStats and aggregateHitterStats"
      pattern: "aggregatePitcherStats"
    - from: "web/lib/charting/playerProfile.ts"
      to: "web/lib/canonicalPlayers.ts"
      via: "canonical slug / alias resolution"
      pattern: "getCanonicalPlayerId"
---

<objective>
Build the profile-facing Live AB aggregation contract.

Purpose: solve the data-model and identity problem first so the player profile tab can render reliable pitcher and hitter views without page-local logic.
</objective>

<context>
@.planning/phases/12.1-live-ab-player-profile-integration/12.1-CONTEXT.md
@.planning/phases/12.1-live-ab-player-profile-integration/12.1-RESEARCH.md
@web/lib/charting/analytics.ts
@web/lib/canonicalPlayers.ts
@web/app/charting/leaderboard/page.tsx
</context>

<tasks>
<task type="code" tdd="true">
  <name>Create the role-aware charting profile helper</name>
  <files>web/lib/charting/playerProfile.ts</files>
  <action>Implement a helper layer that resolves one profile slug into Live AB pitcher aggregates, hitter aggregates, sample-size metadata, and recent-session rows while reusing shared analytics helpers and canonical identity utilities.</action>
  <verify>The helper returns stable shapes for pitcher-only, hitter-only, two-way, and no-data cases without leaking raw DB details into the profile route.</verify>
  <done>Live AB profile data is centralized in one contract instead of being assembled ad hoc in the player page.</done>
</task>

<task type="code" tdd="true">
  <name>Lock the identity and empty-state behavior with tests</name>
  <files>web/lib/charting/playerProfile.test.ts</files>
  <action>Add tests for pitcher identity resolution, hitter name matching, mixed-role players, recent-session ordering, and explicit no-data fallbacks.</action>
  <verify>`npm --prefix web test -- --run lib/charting/playerProfile` passes with coverage for the risky role/identity branches.</verify>
  <done>The later UI plans can trust the helper contract and avoid re-debugging hitter matching during tab work.</done>
</task>
</tasks>

<verification>
1. `npm --prefix web test -- --run lib/charting/playerProfile`
</verification>

<success_criteria>
- Profile-facing Live AB helper exists
- Pitcher and hitter aggregates both resolve through shared analytics
- Role availability and no-data behavior are explicit
- Tests cover the identity join risk before UI integration starts
</success_criteria>

<output>
After completion, create `.planning/phases/12.1-live-ab-player-profile-integration/12.1-01-SUMMARY.md`
</output>
