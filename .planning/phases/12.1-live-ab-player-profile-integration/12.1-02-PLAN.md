---
phase: 12.1-live-ab-player-profile-integration
plan: "02"
type: execute
wave: 2
depends_on: ["12.1-01"]
files_modified:
  - web/app/players/[slug]/page.tsx
  - web/app/players/[slug]/PlayerProfileTabs.tsx
  - web/app/players/[slug]/LiveAbProfilePanel.tsx
autonomous: true
requirements: [PORT-05, SC1, SC2, SC3, SC4]

must_haves:
  truths:
    - "The player profile tab strip exposes a Live AB tab and can deep-link to it."
    - "Pitcher-capable players show a Live AB overview and recent-session history from the shared charting profile helper."
    - "Hitter-capable players show a Live AB overview and recent-session history from the same profile shell."
    - "The tab fits the current player-profile visual language instead of embedding a second leaderboard table."
  artifacts:
    - path: "web/app/players/[slug]/page.tsx"
      provides: "Server-side profile route loading the Live AB profile contract"
    - path: "web/app/players/[slug]/PlayerProfileTabs.tsx"
      provides: "Tab registration and role-aware Live AB entry point"
    - path: "web/app/players/[slug]/LiveAbProfilePanel.tsx"
      provides: "Dedicated Live AB tab UI for profile summaries and session history"
  key_links:
    - from: "web/app/players/[slug]/page.tsx"
      to: "web/lib/charting/playerProfile.ts"
      via: "profile route data loading"
      pattern: "playerProfile"
    - from: "web/app/players/[slug]/PlayerProfileTabs.tsx"
      to: "web/app/players/[slug]/LiveAbProfilePanel.tsx"
      via: "Live AB tab rendering"
      pattern: "LiveAbProfilePanel"
---

<objective>
Integrate the new Live AB tab into the canonical player profile route.

Purpose: surface player-level Live AB overviews for pitchers and hitters from `/players/[slug]` without creating a disconnected charting-only profile experience.
</objective>

<context>
@.planning/phases/12.1-live-ab-player-profile-integration/12.1-CONTEXT.md
@.planning/phases/12.1-live-ab-player-profile-integration/12.1-RESEARCH.md
@web/app/players/[slug]/page.tsx
@web/app/players/[slug]/PlayerProfileTabs.tsx
@web/app/components/leaderboards/LeaderboardChrome.tsx
@web/lib/charting/playerProfile.ts
</context>

<tasks>
<task type="code" tdd="false">
  <name>Extend the profile route to load Live AB data</name>
  <files>web/app/players/[slug]/page.tsx</files>
  <action>Load the new charting profile contract on the server route, expose any required role-awareness flags, and support `?tab=live-ab` deep-linking without breaking the existing profile tabs.</action>
  <verify>The route can render the Live AB tab for players with pitcher data, hitter data, both, or neither.</verify>
  <done>The canonical profile route has the charting data it needs before the UI tab renders.</done>
</task>

<task type="code" tdd="false">
  <name>Add the Live AB tab and profile panel</name>
  <files>web/app/players/[slug]/PlayerProfileTabs.tsx, web/app/players/[slug]/LiveAbProfilePanel.tsx</files>
  <action>Add a new `Live AB` tab that renders compact overview cards, role-aware content, and recent-session links in the same design language as the existing Trackman and Command tabs.</action>
  <verify>The tab reads as a profile overview surface rather than a duplicated leaderboard and supports pitcher-only, hitter-only, and mixed-role players.</verify>
  <done>Live AB becomes a first-class part of the player profile experience.</done>
</task>
</tasks>

<verification>
1. `npm --prefix web test -- --run lib/charting/playerProfile`
2. `npm --prefix web run build`
</verification>

<success_criteria>
- Live AB tab exists on the player profile
- Pitcher and hitter profile views both render from shared charting data
- Deep-linking to the tab is stable
- The UI matches the existing player-profile shell
</success_criteria>

<output>
After completion, create `.planning/phases/12.1-live-ab-player-profile-integration/12.1-02-SUMMARY.md`
</output>
