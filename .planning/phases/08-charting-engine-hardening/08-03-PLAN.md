---
phase: 08-charting-engine-hardening
plan: 03
type: execute
wave: 3
depends_on: [08-01, 08-02]
files_modified:
  - ios/PitchTracker/PitchTrackerTests/PitchTrackerTests.swift
  - ios/PitchTracker/PitchTracker/Models/Models.swift
  - ios/PitchTracker/PitchTracker/Stores/GameStore.swift
autonomous: true
user_setup: []
must_haves:
  truths:
    - Scenario tests lock in count, outs, inning rollover, undo, pitcher-change, and finalize/recovery behavior.
    - Phase 8 correctness claims are backed by repeatable verification rather than manual simulator confidence.
  artifacts:
    - ios/PitchTracker/PitchTrackerTests/PitchTrackerTests.swift
  key_links:
    - Engine rules -> regression coverage
---

# Phase 8 Plan 03: Add Regression Scenarios and Verification

<objective>
Prove the hardened charting engine with scenario-based tests that exercise live scoring sequences and reconstruction paths end to end.
</objective>

## Objective
- **What:** Add focused unit tests for the charting engine and store-level recovery behavior.
- **Why:** Charting logic cannot be called “foolproof” without automated regression coverage around the exact game-state transitions that fail silently.
- **Output:** A scenario matrix covering count progression, PA closure, undo, inning rollover, pitcher changes, and finalize/recovery behavior.

## Context
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-charting-engine-hardening/08-CONTEXT.md

## Tasks
<task type="code">
  <name>Add engine scenario tests</name>
  <files>ios/PitchTracker/PitchTrackerTests/PitchTrackerTests.swift</files>
  <action>Create scenario-heavy tests for strikeouts, walks, bunt-foul strike-three, in-play outs, double plays, undo/reopen flows, and reconstructed state from persisted snapshots.</action>
  <verify>`xcodebuild test` passes with deterministic assertions around count, inning, outs, batter slot, and active segment state.</verify>
  <done>The charting engine has a durable regression suite for the main v1 mechanics.</done>
</task>

<task type="code">
  <name>Close verification gaps exposed by tests</name>
  <files>ios/PitchTracker/PitchTracker/Models/Models.swift, ios/PitchTracker/PitchTracker/Stores/GameStore.swift</files>
  <action>Patch any remaining rule mismatches surfaced by the new tests so the engine and store produce one coherent truth.</action>
  <verify>The full iOS test pass and build pass complete without charting regressions.</verify>
  <done>Phase 8 claims are backed by green scenario coverage.</done>
</task>

## Verification
- `xcodebuild -project ios/PitchTracker/PitchTracker.xcodeproj -scheme PitchTracker -destination 'platform=iOS Simulator,name=iPad Air 11-inch (M3)' test`

## Success Criteria
- [ ] Scenario tests cover the primary v1 charting mechanics
- [ ] iOS build/test verification passes against the hardened engine
