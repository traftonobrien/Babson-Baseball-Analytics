---
phase: 21-pbp-parser-foundation
plan: 03
type: execute
wave: 2
depends_on:
  - 21-01
  - 21-02
files_modified:
  - web/lib/runExpectancy/types.ts
  - web/lib/runExpectancy/pbpParser.ts
  - web/lib/runExpectancy/pbpParser.test.ts
  - web/public/data/run-expectancy/re_game_map.json
autonomous: true
requirements:
  - PBP-04
  - PBP-07
  - PBP-01
  - PBP-06

must_haves:
  truths:
    - "Per-pitch count snapshots are derived by walking the Sidearm sequence string from 0-0, not by treating the final count as pre-pitch state"
    - "`re_game_map.json` maps every known Sidearm gameId to date, opponent, homeAway, and doubleheader suffix"
    - "The Phase 21 season gate is executable and fails if fewer than 15 games pass run-total validation"
    - "Edge-case tests cover DP, sac fly, IBB, HBP, duplicate text across innings, and count-sequence reconstruction"
  artifacts:
    - path: "web/lib/runExpectancy/pbpParser.ts"
      provides: "Final Phase 21 season-corpus builder with count snapshots and validation tally"
    - path: "web/lib/runExpectancy/pbpParser.test.ts"
      provides: "Edge-case and count-reconstruction coverage"
    - path: "web/public/data/run-expectancy/re_game_map.json"
      provides: "Sidearm gameId bridge metadata for doubleheaders and later charting joins"
  key_links:
    - from: "web/lib/runExpectancy/pbpParser.ts"
      to: "web/public/data/run-expectancy/re_game_map.json"
      via: "game identity metadata for corpus output"
      pattern: "gameId -> { date, opponent, homeAway, suffix }"
    - from: "web/lib/runExpectancy/pbpParser.ts"
      to: "web/lib/spraychart/zoneMapper.ts"
      via: "shared understanding that Sidearm count suffixes encode final count + pitch sequence"
      pattern: "count + pitchSequence"
---

<objective>
Finish Phase 21 by adding count-state reconstruction, join metadata, and the live season gate that proves the parser is trustworthy enough for Phase 22.

Purpose: The parser is only phase-complete when it can reconstruct per-pitch count snapshots, identify each Sidearm game unambiguously for later joins, and demonstrate season-wide validation quality against the user’s `>=15/19` threshold.

Output: a final Phase 21 parser that emits count snapshots, writes `re_game_map.json`, and exposes a callable season validation gate.
</objective>

<execution_context>
@/Users/traftonobrien/.codex/get-shit-done/workflows/execute-plan.md
@/Users/traftonobrien/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/21-pbp-parser-foundation/21-CONTEXT.md
@.planning/phases/21-pbp-parser-foundation/21-RESEARCH.md
@.planning/phases/21-pbp-parser-foundation/21-VALIDATION.md
@tasks/codex-handoff-re288.md
@web/lib/runExpectancy/pbpParser.ts
@web/lib/runExpectancy/pbpParser.test.ts
@web/lib/spraychart/scraper.ts

<interfaces>
Final Phase 21 requirements:
- Count snapshots must be derivable for every parsed PA with a Sidearm sequence string.
- `re_game_map.json` must cover the full known 2026 schedule and preserve doubleheader suffixes.
- The parser must expose a callable season-build function that can be used by an automated gate command immediately, even before Phase 22 adds a top-level script.
- Do not add `build_re_matrix.ts` or edit `/charting/ohtwo` in this plan.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add count-sequence walking and edge-case unit coverage</name>
  <files>web/lib/runExpectancy/pbpParser.ts, web/lib/runExpectancy/pbpParser.test.ts, web/lib/runExpectancy/types.ts</files>
  <action>
Implement the pure count reconstruction layer that walks Sidearm pitch sequences from `0-0` through each character and emits count snapshots for each pitch step.

Required coverage:
- the handoff examples,
- IBB/HBP/walk-like endings,
- foul-ball handling with two strikes,
- the "state just before the last pitch" case needed later for 0-2 fastball delta-RE.

Keep the count walker pure and independently testable from live HTML parsing.
  </action>
  <verify>
    <automated>cd /Users/traftonobrien/Desktop/pitch-tracker && npm --prefix web exec vitest run lib/runExpectancy/pbpParser.test.ts --testNamePattern "count snapshots|pitch sequence|0-2"</automated>
  </verify>
  <done>The parser can derive per-pitch count states from Sidearm sequence strings and tests cover the known edge cases.</done>
</task>

<task type="auto">
  <name>Task 2: Author `re_game_map.json` and wire season metadata into the parser output</name>
  <files>web/public/data/run-expectancy/re_game_map.json, web/lib/runExpectancy/pbpParser.ts, web/lib/runExpectancy/types.ts</files>
  <action>
Create the game-id bridge file and make the parser consume or emit metadata consistent with it.

Requirements:
- one entry per known 2026 Sidearm game,
- fields include `gameId`, `date`, `opponent`, `homeAway`, and `suffix`,
- doubleheader games are distinguishable even if the opponent/date text is otherwise the same.

Use the existing known-game list and roadmap/handoff constraints rather than inventing fuzzy metadata later in Phase 23.
  </action>
  <verify>
    <automated>cd /Users/traftonobrien/Desktop/pitch-tracker && test -f web/public/data/run-expectancy/re_game_map.json && rg -n '"suffix"|"homeAway"|"gameId"' web/public/data/run-expectancy/re_game_map.json</automated>
  </verify>
  <done>The game-id mapping file exists and preserves the metadata Phase 23 will need for charting joins.</done>
</task>

<task type="auto">
  <name>Task 3: Expose and run the Phase 21 season validation gate</name>
  <files>web/lib/runExpectancy/pbpParser.ts, web/lib/runExpectancy/pbpParser.test.ts</files>
  <action>
Expose a callable season-build function that:
- discovers the 2026 Sidearm URLs,
- parses each game,
- reports passing vs failed games/innings,
- fails the gate when fewer than 15 games pass run-total validation.

The output should be structured enough for the Phase 21 summary to report the exact pass/fail tally and the main failure modes.
  </action>
  <verify>
    <automated>cd /Users/traftonobrien/Desktop/pitch-tracker && npx tsx -e "import { discoverGameUrls } from './web/lib/spraychart/scraper.ts'; import { buildSeasonRunExpectancyCorpus } from './web/lib/runExpectancy/pbpParser.ts'; const urls = await discoverGameUrls(2026); const result = await buildSeasonRunExpectancyCorpus(urls); console.log(JSON.stringify({ totalGames: result.totalGames, passingGames: result.passingGames, failedGames: result.failedGames }, null, 2)); if (result.passingGames < 15) process.exit(1);"</automated>
  </verify>
  <done>The parser has a real executable season gate and Phase 21 can be marked complete only when the pass threshold is met.</done>
</task>

</tasks>

<verification>
1. `npm --prefix web exec vitest run lib/runExpectancy/pbpParser.test.ts --testNamePattern "count snapshots|pitch sequence|0-2"` — count reconstruction works
2. `test -f web/public/data/run-expectancy/re_game_map.json && rg -n '"suffix"|"homeAway"|"gameId"' web/public/data/run-expectancy/re_game_map.json` — join metadata exists
3. `npx tsx -e "... buildSeasonRunExpectancyCorpus ... if (result.passingGames < 15) process.exit(1)"` — the live season gate passes at the required threshold
4. `npm --prefix web exec vitest run lib/runExpectancy/pbpParser.test.ts lib/spraychart/zoneMapper.test.ts && npm --prefix web run build` — final Phase 21 verification
</verification>

<success_criteria>
- Count snapshots are reconstructed from Sidearm pitch sequences correctly
- `re_game_map.json` exists and covers doubleheader identity cleanly
- The Phase 21 season gate is executable and enforces the `>=15/19` threshold
- Final parser tests and build pass
</success_criteria>

<output>
After completion, create `.planning/phases/21-pbp-parser-foundation/21-03-SUMMARY.md`
</output>
