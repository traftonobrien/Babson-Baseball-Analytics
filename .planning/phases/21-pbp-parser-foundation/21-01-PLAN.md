---
phase: 21-pbp-parser-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - web/lib/spraychart/scraper.ts
  - web/lib/runExpectancy/types.ts
  - web/lib/runExpectancy/pbpParser.ts
  - web/lib/runExpectancy/pbpParser.test.ts
autonomous: true
requirements:
  - PBP-01
  - PBP-05

must_haves:
  truths:
    - "Phase 21 reuses the existing Sidearm fetch/discovery path instead of inventing a second scraper stack"
    - "Raw play extraction preserves inning and half-inning context for both teams before any baseball-state parsing begins"
    - "Duplicate play rows are keyed by inning + half-inning + play text, not plain play text"
    - "The spray chart pipeline keeps working after the helper exports are added"
  artifacts:
    - path: "web/lib/spraychart/scraper.ts"
      provides: "Exported Sidearm HTML/raw-play helpers that remain backward-compatible for spray charts"
    - path: "web/lib/runExpectancy/types.ts"
      provides: "Typed contracts for raw Sidearm rows and half-inning grouping"
    - path: "web/lib/runExpectancy/pbpParser.ts"
      provides: "Raw Phase 21 ingestion entrypoints for RE-specific half-inning extraction"
    - path: "web/lib/runExpectancy/pbpParser.test.ts"
      provides: "Fixture coverage for raw extraction and dedup semantics"
  key_links:
    - from: "web/lib/runExpectancy/pbpParser.ts"
      to: "web/lib/spraychart/scraper.ts"
      via: "shared Sidearm fetch/raw extraction helpers"
      pattern: "fetchPlayByPlayHtml + extractPlayLines"
    - from: "web/lib/runExpectancy/pbpParser.ts"
      to: "web/lib/runExpectancy/types.ts"
      via: "raw half-inning and play-row contracts"
      pattern: "RawPbp* types"
---

<objective>
Build the raw Sidearm ingestion foundation for run expectancy without disturbing the existing spray chart pipeline.

Purpose: Phase 21 needs a reliable, structured representation of Sidearm play-by-play before any base-state or count logic can be trusted. This plan establishes the raw extraction seam and fixes the dedup-context bug at the source.

Output: a new `web/lib/runExpectancy/` parser foundation that can fetch Sidearm HTML, extract raw play rows for both teams, and preserve inning/half-inning-aware deduplication.
</objective>

<execution_context>
@/Users/traftonobrien/.codex/get-shit-done/workflows/execute-plan.md
@/Users/traftonobrien/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/21-pbp-parser-foundation/21-CONTEXT.md
@.planning/phases/21-pbp-parser-foundation/21-RESEARCH.md
@.planning/phases/21-pbp-parser-foundation/21-VALIDATION.md
@tasks/codex-handoff-re288.md
@web/lib/spraychart/scraper.ts

<interfaces>
Current constraints from the live codebase and handoff:
- `discoverGameUrls(2026)` already exists and should stay the season discovery entrypoint.
- `web/scripts/scrape_spray_charts.ts` must remain unchanged.
- Only export the two existing helpers needed by the new parser from `web/lib/spraychart/scraper.ts`; do not restructure the spray-chart file beyond that.
- The new raw extractor must preserve inning and half-inning context so identical play text in different innings survives deduplication.
- This plan is raw-ingestion only; do not implement base-state transitions or matrix math yet.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Expose the reusable Sidearm helper seam and add RE raw-ingestion types</name>
  <files>web/lib/spraychart/scraper.ts, web/lib/runExpectancy/types.ts, web/lib/runExpectancy/pbpParser.ts</files>
  <action>
Export the two existing spray-chart helpers the RE parser needs from `web/lib/spraychart/scraper.ts`, then create `web/lib/runExpectancy/types.ts` and the first `web/lib/runExpectancy/pbpParser.ts` entrypoints around them.

Requirements for the new raw layer:
- Represent raw play rows with inning, halfInning, playText, and a dedup key that includes inning context.
- Parse both teams' play rows, not only Babson batting halves.
- Keep the RE parser independent from spray-chart-only concepts like Babson roster filtering and batted-ball classification.

Constraints:
- Preserve the current `scrapeBoxScore()` behavior for spray charts.
- Do not add matrix, base-state, or run-value logic in this task.
  </action>
  <verify>
    <automated>cd /Users/traftonobrien/Desktop/pitch-tracker && npm --prefix web run build</automated>
  </verify>
  <done>The RE parser has a structured raw-ingestion layer and the spray-chart scraper exposes the helper seam without behavior regressions.</done>
</task>

<task type="auto">
  <name>Task 2: Add fixture coverage for raw extraction and inning-aware deduplication</name>
  <files>web/lib/runExpectancy/pbpParser.test.ts, web/lib/runExpectancy/pbpParser.ts</files>
  <action>
Add unit tests around the real Phase 21 failure modes:
- extracting play rows from Sidearm-like HTML into half-innings,
- preserving duplicate play text when inning/half differs,
- rejecting plain-text-only dedup semantics,
- preserving both teams' rows instead of only Babson batting halves.

Use compact fixture HTML strings in the test file so the parser can be iterated without live network access.
  </action>
  <verify>
    <automated>cd /Users/traftonobrien/Desktop/pitch-tracker && npm --prefix web exec vitest run lib/runExpectancy/pbpParser.test.ts --testNamePattern "raw extraction|dedup"</automated>
  </verify>
  <done>Focused tests prove raw extraction preserves inning context and no longer collapses identical play descriptions from different innings.</done>
</task>

</tasks>

<verification>
1. `npm --prefix web exec vitest run lib/runExpectancy/pbpParser.test.ts --testNamePattern "raw extraction|dedup"` — raw extractor tests pass
2. `npm --prefix web run build` — the app still builds after the helper exports and new parser module land
</verification>

<success_criteria>
- `web/lib/spraychart/scraper.ts` exposes the reusable Sidearm helper seam without breaking spray charts
- The new RE parser can extract structured raw half-inning rows for both teams
- Deduplication now depends on inning context, not only play text
- Raw-ingestion tests and build both pass
</success_criteria>

<output>
After completion, create `.planning/phases/21-pbp-parser-foundation/21-01-SUMMARY.md`
</output>
