---
phase: 21-pbp-parser-foundation
plan: 02
type: execute
wave: 1
depends_on:
  - 21-01
files_modified:
  - web/lib/runExpectancy/pbpParser.ts
  - web/lib/runExpectancy/pbpParser.test.ts
autonomous: true
requirements:
  - PBP-02
  - PBP-03
  - PBP-06

must_haves:
  truths:
    - "Semicolon-delimited sub-events are processed sequentially within one PA"
    - "Base state and outs reset correctly at each half-inning boundary"
    - "Parsed half-inning run totals are compared against the box score `r` column and failures are excluded/logged"
    - "Phase 22 will receive only validated innings, never silent bad data"
  artifacts:
    - path: "web/lib/runExpectancy/pbpParser.ts"
      provides: "Sequential base/out/run state machine plus inning validation reporting"
    - path: "web/lib/runExpectancy/pbpParser.test.ts"
      provides: "Regression coverage for semicolon runner movement, outs, and failed-inning exclusion"
  key_links:
    - from: "web/lib/runExpectancy/pbpParser.ts"
      to: "tasks/codex-handoff-re288.md"
      via: "compound-play and run-total validation rules"
      pattern: "semicolon events + ≥15/19 validation gate"
    - from: "web/lib/runExpectancy/pbpParser.ts"
      to: "web/lib/runExpectancy/types.ts"
      via: "validated inning and parsed PA record contracts"
      pattern: "ParsedRunExpectancy* types"
---

<objective>
Implement the baseball-state machine that makes raw Sidearm play rows trustworthy for run expectancy.

Purpose: Raw rows alone are insufficient. Phase 21 needs a deterministic base/out/run transition layer that can survive compound Sidearm lines and reject innings whose parsed run totals do not match the official box score.

Output: a parser that converts raw half-innings into validated PA/state records with explicit pass/fail reporting.
</objective>

<execution_context>
@/Users/traftonobrien/.codex/get-shit-done/workflows/execute-plan.md
@/Users/traftonobrien/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/21-pbp-parser-foundation/21-CONTEXT.md
@.planning/phases/21-pbp-parser-foundation/21-RESEARCH.md
@.planning/phases/21-pbp-parser-foundation/21-VALIDATION.md
@tasks/codex-handoff-re288.md
@web/lib/runExpectancy/pbpParser.ts
@web/lib/runExpectancy/pbpParser.test.ts

<interfaces>
State-machine non-negotiables:
- Base-state transitions must follow sub-event order inside one Sidearm row.
- Outs may change within the PA anchor text and within later sub-events; the state machine must capture the final half-inning state deterministically.
- Failed half-innings must be represented as failed validation output, not discarded silently in-place.
- This plan still stops short of season-wide count snapshots and `re_game_map.json`; those belong to 21-03.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build the sequential sub-event state machine</name>
  <files>web/lib/runExpectancy/pbpParser.ts, web/lib/runExpectancy/pbpParser.test.ts</files>
  <action>
Extend `pbpParser.ts` so one raw half-inning can be parsed into ordered PA/state records with:
- current outs before and after the play,
- current base state before and after the play,
- runs scored on the play,
- support for compound rows split on semicolons.

Cover the known Phase 21 edge cases in tests:
- double play,
- sac fly with a scoring runner,
- hit by pitch / walk style runner forcing,
- repeated runner-advance clauses after a hit.

Keep the implementation deterministic and typed; avoid hand-wavy string mutation.
  </action>
  <verify>
    <automated>cd /Users/traftonobrien/Desktop/pitch-tracker && npm --prefix web exec vitest run lib/runExpectancy/pbpParser.test.ts --testNamePattern "semicolon|base state|outs reset"</automated>
  </verify>
  <done>The parser now reconstructs sequential base/out state correctly across compound Sidearm play rows.</done>
</task>

<task type="auto">
  <name>Task 2: Add half-inning run-total validation and explicit failed-inning exclusion</name>
  <files>web/lib/runExpectancy/pbpParser.ts, web/lib/runExpectancy/pbpParser.test.ts</files>
  <action>
Add validation reporting that compares parsed half-inning runs to the official box-score `r` column. The parser should:
- mark a half-inning as validated or failed,
- exclude failed innings from the returned "usable for matrix" collection,
- retain failure details for logging/reporting.

Tests should prove both branches:
- a passing inning stays eligible,
- a mismatched inning is excluded and surfaced.
  </action>
  <verify>
    <automated>cd /Users/traftonobrien/Desktop/pitch-tracker && npm --prefix web exec vitest run lib/runExpectancy/pbpParser.test.ts --testNamePattern "run-total validation|exclude failing inning"</automated>
  </verify>
  <done>The parser emits explicit validation results and never silently includes a bad inning in downstream data.</done>
</task>

</tasks>

<verification>
1. `npm --prefix web exec vitest run lib/runExpectancy/pbpParser.test.ts --testNamePattern "semicolon|base state|outs reset"` — state-machine tests pass
2. `npm --prefix web exec vitest run lib/runExpectancy/pbpParser.test.ts --testNamePattern "run-total validation|exclude failing inning"` — validation reporting works
</verification>

<success_criteria>
- Compound Sidearm rows update runners, outs, and runs in sequence
- Half-inning boundaries reset state cleanly
- Failed innings are explicitly reported and excluded from future RE computation
- Focused parser tests pass for state transitions and validation
</success_criteria>

<output>
After completion, create `.planning/phases/21-pbp-parser-foundation/21-02-SUMMARY.md`
</output>
