---
phase: 11-session-overview-enhancements
plan: "02"
type: execute
wave: 2
depends_on: ["11-01"]
files_modified:
  - web/app/charting/_components/ChartingZoneHeatmap.tsx
  - web/lib/charting/sessionOverview.test.ts
  - web/app/charting/games/[id]/page.tsx
autonomous: true
requirements: [SC2, SC4]

must_haves:
  truths:
    - "A reusable 14-cell catcher-view heat map component exists for charting review surfaces."
    - "Pitcher breakdown cards render a frequency heat map colored by relative cell usage."
    - "The heat map renders a graceful empty shell when there are no located pitches."
  artifacts:
    - path: "web/app/charting/_components/ChartingZoneHeatmap.tsx"
      provides: "Reusable discrete 14-cell zone frequency heat map"
    - path: "web/app/charting/games/[id]/page.tsx"
      provides: "Pitcher cards upgraded with the reusable heat map"
  key_links:
    - from: "web/app/charting/games/[id]/page.tsx"
      to: "web/app/charting/_components/ChartingZoneHeatmap.tsx"
      via: "pitcher card rendering"
      pattern: "ChartingZoneHeatmap"
---

<objective>
Add the reusable 14-cell charting heat map and wire it into the pitcher breakdown section.

Purpose: satisfy the Phase 11 heat-map requirement with a component that can be reused by the hitter section in the next plan.
</objective>

<context>
@.planning/phases/11-session-overview-enhancements/11-CONTEXT.md
@.planning/phases/11-session-overview-enhancements/11-RESEARCH.md
@web/app/components/Heatmap.tsx
@web/app/components/MissHeatmap.tsx
@web/app/charting/games/[id]/page.tsx
</context>

<tasks>
<task type="code" tdd="false">
  <name>Create the reusable charting zone heat map component</name>
  <files>web/app/charting/_components/ChartingZoneHeatmap.tsx</files>
  <action>Implement a discrete catcher-view 14-cell frequency map that accepts cell-count data, colors by relative usage, and renders an explicit empty state when there are no located pitches.</action>
  <verify>The component can render both populated and empty states without layout breakage.</verify>
  <done>The charting route has a reusable zone-frequency primitive instead of route-specific SVG duplication.</done>
</task>

<task type="code" tdd="false">
  <name>Embed the heat map into the pitcher cards</name>
  <files>web/app/charting/games/[id]/page.tsx, web/lib/charting/sessionOverview.test.ts</files>
  <action>Render the new heat map inside each pitcher breakdown card using the segment cell-frequency data produced by the overview helper layer.</action>
  <verify>Each pitcher card shows a heat map when located pitches exist and a readable fallback when they do not.</verify>
  <done>Pitcher review cards now satisfy the full per-segment visual requirement.</done>
</task>
</tasks>

<verification>
1. `npm --prefix web test -- --run lib/charting/sessionOverview`
2. `npm --prefix web run build`
</verification>

<success_criteria>
- Reusable heat map component exists
- Pitcher cards render the 14-cell frequency map
- Empty-state heat maps are explicit and stable
</success_criteria>

<output>
After completion, create `.planning/phases/11-session-overview-enhancements/11-02-SUMMARY.md`
</output>
