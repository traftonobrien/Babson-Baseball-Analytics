---
phase: 11-session-overview-enhancements
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - web/lib/charting/sessionOverview.ts
  - web/lib/charting/sessionOverview.test.ts
  - web/app/charting/games/[id]/page.tsx
autonomous: true
requirements: [SC1, SC4]

must_haves:
  truths:
    - "The game detail page shows one pitcher breakdown card for every pitcher segment in the snapshot."
    - "Pitcher cards reuse Phase 10 analytics outputs instead of duplicating formula logic."
    - "Each pitcher card shows pitch mix, Strike%, Zone%, Whiff%, Chase%, FPS%, and PA outcome counts."
    - "Pitcher sections degrade gracefully when there are no segment pitches or no closed PAs."
  artifacts:
    - path: "web/lib/charting/sessionOverview.ts"
      provides: "Pitcher overview view-model helpers and PA outcome bucketing"
    - path: "web/lib/charting/sessionOverview.test.ts"
      provides: "Unit coverage for pitcher grouping and outcomes"
    - path: "web/app/charting/games/[id]/page.tsx"
      provides: "Pitcher breakdown section rendered below the summary row"
  key_links:
    - from: "web/lib/charting/sessionOverview.ts"
      to: "web/lib/charting/analytics.ts"
      via: "computeSegmentStats_pure"
      pattern: "computeSegmentStats_pure"
---

<objective>
Build the pitcher side of the Phase 11 session overview.

Purpose: deliver the per-pitcher breakdown cards on `/charting/games/[id]` using the shared analytics engine and compact outcome summaries.
</objective>

<context>
@.planning/phases/11-session-overview-enhancements/11-CONTEXT.md
@.planning/phases/11-session-overview-enhancements/11-RESEARCH.md
@web/app/charting/games/[id]/page.tsx
@web/lib/charting/analytics.ts
@web/lib/charting/snapshot.ts
</context>

<tasks>
<task type="code" tdd="true">
  <name>Build session overview helpers for pitcher breakdowns</name>
  <files>web/lib/charting/sessionOverview.ts, web/lib/charting/sessionOverview.test.ts</files>
  <action>Create helpers that derive segment-specific pitches/PAs, compute pitcher stats from the Phase 10 pure analytics engine, and bucket PA outcomes into K / BB / HBP / hits / outs counts.</action>
  <verify>Unit tests cover segment grouping, K/KL strikeouts, hit bucketing, and graceful empty-state outputs.</verify>
  <done>Pitcher breakdown data is computed in one helper layer rather than ad hoc in JSX.</done>
</task>

<task type="code" tdd="false">
  <name>Render the pitcher breakdown section on the game detail page</name>
  <files>web/app/charting/games/[id]/page.tsx</files>
  <action>Add a new section below the existing summary cards that renders one card per pitcher segment with pitch mix, rate stats, and outcome summaries while preserving the current page chrome.</action>
  <verify>The page shows one pitcher card per segment and renders an explicit empty state when a segment has no charted pitches.</verify>
  <done>The route now presents meaningful per-pitcher review context before the pitch log.</done>
</task>
</tasks>

<verification>
1. `npm --prefix web test -- --run lib/charting/sessionOverview`
2. `npm --prefix web run build`
</verification>

<success_criteria>
- Pitcher cards render for every segment in the snapshot
- Rate stats come from shared analytics helpers
- Outcome counts are readable and correctly bucketed
- Empty states render without misleading zeros or broken markup
</success_criteria>

<output>
After completion, create `.planning/phases/11-session-overview-enhancements/11-01-SUMMARY.md`
</output>
