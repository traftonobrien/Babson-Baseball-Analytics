---
phase: 11-session-overview-enhancements
plan: "03"
type: execute
wave: 3
depends_on: ["11-01", "11-02"]
files_modified:
  - web/lib/charting/sessionOverview.ts
  - web/lib/charting/sessionOverview.test.ts
  - web/app/charting/games/[id]/page.tsx
autonomous: true
requirements: [SC1, SC2, SC3, SC4]

must_haves:
  truths:
    - "The game detail page shows one hitter breakdown card for every hitter with a charted plate appearance."
    - "Hitter cards show pitches seen, Chase%, Contact%, K/BB context, pitch-type split context, and a zone coverage map."
    - "Both new sections degrade gracefully when no pitches or PAs are recorded."
    - "The full web test suite and production build are green after the Phase 11 route updates."
  artifacts:
    - path: "web/lib/charting/sessionOverview.ts"
      provides: "Hitter grouping and view-model helpers"
    - path: "web/lib/charting/sessionOverview.test.ts"
      provides: "Coverage for hitter grouping, outcomes, and zone-frequency derivation"
    - path: "web/app/charting/games/[id]/page.tsx"
      provides: "Completed Phase 11 game detail enhancements"
  key_links:
    - from: "web/lib/charting/sessionOverview.ts"
      to: "web/lib/charting/analytics.ts"
      via: "computeHitterStats_pure"
      pattern: "computeHitterStats_pure"
---

<objective>
Finish the Phase 11 session overview by adding the hitter section and running the full route verification gate.

Purpose: complete the `/charting/games/[id]` enhancement so both pitchers and hitters are reviewable on one page.
</objective>

<context>
@.planning/phases/11-session-overview-enhancements/11-CONTEXT.md
@.planning/phases/11-session-overview-enhancements/11-RESEARCH.md
@web/lib/charting/sessionOverview.ts
@web/app/charting/_components/ChartingZoneHeatmap.tsx
@web/app/charting/games/[id]/page.tsx
@web/lib/charting/analytics.ts
</context>

<tasks>
<task type="code" tdd="true">
  <name>Extend the overview helper layer for hitter cards</name>
  <files>web/lib/charting/sessionOverview.ts, web/lib/charting/sessionOverview.test.ts</files>
  <action>Group snapshot plate appearances by hitter, derive hitter zone-frequency maps and display-ready split summaries, and cover the grouping/order/empty-state behavior with tests.</action>
  <verify>Tests prove hitter grouping is stable, zone maps ignore null cells, and open PAs do not corrupt hitter outcome summaries.</verify>
  <done>The route can render hitter cards from a stable helper contract.</done>
</task>

<task type="code" tdd="false">
  <name>Render the hitter breakdown section and verify the route</name>
  <files>web/app/charting/games/[id]/page.tsx</files>
  <action>Add the per-hitter section below the pitcher section, render the reusable heat map and hitter split stats, and polish the route spacing and empty states so the full Phase 11 experience reads coherently.</action>
  <verify>`npm --prefix web test -- --run` and `npm --prefix web run build` both pass.</verify>
  <done>The existing charting game detail page now satisfies all Phase 11 success criteria.</done>
</task>
</tasks>

<verification>
1. `npm --prefix web test -- --run lib/charting/sessionOverview`
2. `npm --prefix web test -- --run`
3. `npm --prefix web run build`
</verification>

<success_criteria>
- Hitter cards render for every charted hitter
- Hitter stats and split summaries reuse the shared analytics engine
- Reusable heat map serves both pitcher and hitter sections
- Full test suite and build remain green
</success_criteria>

<output>
After completion, create `.planning/phases/11-session-overview-enhancements/11-03-SUMMARY.md`
</output>
