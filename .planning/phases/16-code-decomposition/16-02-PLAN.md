---
phase: 16-code-decomposition
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - web/app/charting/insights/LiveAbInsightsExplorer.tsx
  - web/app/charting/insights/_components/*
  - web/app/charting/insights/_lib/*
  - web/app/charting/insights/page.tsx
autonomous: true
requirements:
  - CODE-02

must_haves:
  truths:
    - "LiveAbInsightsExplorer.tsx becomes a thin route-local composition file instead of a 2863-line monolith"
    - "Extracted insights modules are each under 500 lines"
    - "Current explorer behavior is preserved: search, filters, URL-backed state, view switching, zone/cell/row/column selection, pitch mix, summary table, and takeaways"
    - "The decomposition stays route-local and does not expand into a broader shared comparison-platform rewrite"
  artifacts:
    - path: "web/app/charting/insights/LiveAbInsightsExplorer.tsx"
      provides: "Thin root client explorer that composes extracted route-local modules"
    - path: "web/app/charting/insights/_components"
      provides: "Explorer UI panels and route-local presentational modules"
    - path: "web/app/charting/insights/_lib"
      provides: "Explorer-local formatting, selection, and zone-display helpers"
  key_links:
    - from: "web/app/charting/insights/page.tsx"
      to: "web/app/charting/insights/LiveAbInsightsExplorer.tsx"
      via: "server loader passes entries + view to the client explorer"
      pattern: "<LiveAbInsightsExplorer"
    - from: "web/app/charting/insights/LiveAbInsightsExplorer.tsx"
      to: "web/app/charting/insights/explorerState.ts"
      via: "URL/query read + write contract"
      pattern: "readHitterExplorerQuery"
---

<objective>
Decompose `LiveAbInsightsExplorer.tsx` into route-local modules while preserving the existing filter/search/query behavior and insights presentation.

Purpose: `LiveAbInsightsExplorer.tsx` currently mixes static constants, zone-display logic, view-specific helper functions, query-driven state, and large presentational panels in one 2863-line file. This plan splits it into route-local modules that are easier to navigate without changing the explorer experience.

Output: a decomposed insights explorer with route-local modules under `web/app/charting/insights/`.
</objective>

<execution_context>
@/Users/traftonobrien/.claude/get-shit-done/workflows/execute-plan.md
@/Users/traftonobrien/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/16-code-decomposition/16-CONTEXT.md
@.planning/phases/16-code-decomposition/16-VALIDATION.md
@web/app/charting/insights/page.tsx
@web/app/charting/insights/LiveAbInsightsExplorer.tsx
@web/app/charting/insights/explorerState.ts
@web/lib/charting/playerComparison.ts
@web/lib/charting/pitcherComparison.ts
@web/lib/selectedPlayer.tsx

<interfaces>
<!-- Preserve these route-local contracts during extraction. -->

Server/route contract:
- `web/app/charting/insights/page.tsx` remains the thin async loader that normalizes `view`, loads entries, and renders `<LiveAbInsightsExplorer key={`${view}:${explorerParams.toString()}`}>`.

Query-state contract:
- `explorerState.ts` already owns the read/build query helpers; keep this contract intact.
- Preserve URL-backed state, router replace behavior, and browser back/forward handling.

Known inline extraction candidates:
- `SearchResultCard`
- `FilterSelect`
- `ComparisonViewToggle`
- `VelocityRangeControl`
- `MetricToggle`
- `ZoneSectionRegion`
- `ZoneDisplayModeToggle`
- `MiniStat`
- `PitchMixPanel`
- `ZoneCanvas`
- `SummaryTable`
- `EmptyState`
- `derivePitcherExplorerTakeaways`
- `deriveHitterExplorerTakeaways`

Scope guard:
- Do not use this plan to merge the explorer with player-profile Live AB UI or create a broad cross-surface comparison component library.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract route-local explorer panels, constants, and display helpers</name>
  <files>web/app/charting/insights/LiveAbInsightsExplorer.tsx, web/app/charting/insights/_components/*, web/app/charting/insights/_lib/*</files>
  <action>
Create route-local decomposition folders for the insights explorer, such as:
- `web/app/charting/insights/_components/`
- `web/app/charting/insights/_lib/`

Extract the obvious inline view modules and static display helpers first:
- search/filter controls
- zone display pieces
- pitch-mix and summary table panels
- empty-state / utility display blocks
- zone-layout constants, formatting helpers, and takeaways helpers that are specific to this route

Guidelines:
- Keep the decomposition route-local; do not move these modules into cross-app shared folders.
- Keep each extracted module under 500 lines.
- Preserve the visible explorer behavior, current copy, and current view/filter/query flow.
  </action>
  <verify>
    <automated>test $(wc -l < /Users/traftonobrien/Desktop/pitch-tracker/web/app/charting/insights/LiveAbInsightsExplorer.tsx) -lt 1000 && find /Users/traftonobrien/Desktop/pitch-tracker/web/app/charting/insights/_components /Users/traftonobrien/Desktop/pitch-tracker/web/app/charting/insights/_lib -type f \( -name '*.ts' -o -name '*.tsx' \) -exec sh -c 'n=$(wc -l < "$1"); [ "$n" -lt 500 ] || { echo "$1:$n"; exit 1; }' _ {} \;</automated>
  </verify>
  <done>LiveAbInsightsExplorer.tsx is materially smaller and extracted route-local explorer modules exist with each module under 500 lines.</done>
</task>

<task type="auto">
  <name>Task 2: Thin the root explorer and preserve query-driven behavior</name>
  <files>web/app/charting/insights/LiveAbInsightsExplorer.tsx, web/app/charting/insights/page.tsx, web/app/charting/insights/_components/*, web/app/charting/insights/_lib/*</files>
  <action>
Finish the explorer decomposition by leaving `LiveAbInsightsExplorer.tsx` as the thin client composition/root file.

Preserve these behaviors while thinning the root:
- search result selection and pinned player behavior
- view switching between hitters/pitchers
- URL-backed filters and replace-state updates
- popstate/back-forward restoration
- zone bucket / row / column / cell selection logic
- current summary, pitch-mix, and takeaway rendering behavior

Do not expand scope into broader UI reuse beyond this route.

After extraction, run the focused comparison/explorer verification commands and fix any decomposition regressions before finishing the task.
  </action>
  <verify>
    <automated>cd /Users/traftonobrien/Desktop/pitch-tracker && npm --prefix web test -- --run lib/charting/explorerState.test.ts lib/charting/playerComparison.test.ts lib/charting/pitcherComparison.test.ts && npm --prefix web run build</automated>
  </verify>
  <done>Explorer decomposition preserves route-local query behavior, focused comparison tests pass, and the build succeeds.</done>
</task>

</tasks>

<verification>
1. `test $(wc -l < web/app/charting/insights/LiveAbInsightsExplorer.tsx) -lt 1000` — root explorer file is below the ceiling
2. `find web/app/charting/insights/_components web/app/charting/insights/_lib -type f ...` — every extracted module is under 500 lines
3. `npm --prefix web test -- --run lib/charting/explorerState.test.ts lib/charting/playerComparison.test.ts lib/charting/pitcherComparison.test.ts` — focused explorer/comparison tests pass
4. `npm --prefix web run build` — build exits 0
</verification>

<success_criteria>
- `LiveAbInsightsExplorer.tsx` is no longer a mega-file and now acts as a thin root composition
- Extracted route-local explorer modules are all under 500 lines
- Current explorer query/filter/selection behavior is preserved
- Focused explorer/comparison tests and build both pass
</success_criteria>

<output>
After completion, create `.planning/phases/16-code-decomposition/16-02-SUMMARY.md`
</output>
