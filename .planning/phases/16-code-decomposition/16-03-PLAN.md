---
phase: 16-code-decomposition
plan: 03
type: execute
wave: 2
depends_on:
  - 16-01
  - 16-02
files_modified:
  - web/lib/charting/live.ts
  - web/app/players/[slug]/PlayerProfileTabs.tsx
  - web/app/players/[slug]/page.tsx
  - web/lib/charting/*
  - web/app/players/[slug]/*
autonomous: true
requirements:
  - CODE-03

must_haves:
  truths:
    - "A source-only line-count audit across web/app, web/lib, and web/tests shows no file over 1000 lines at phase completion"
    - "Any remaining ceiling offenders are decomposed using the same behavior-freeze, feature-local posture established in Phase 16 context"
    - "The final web/ ceiling audit excludes node_modules and build artifacts"
    - "Focused regression checks and a production build pass after the remaining decompositions"
  artifacts:
    - path: "web/lib/charting/live.ts"
      provides: "No longer a >1000-line ceiling offender if it remains over the limit after 16-01/02"
    - path: "web/app/players/[slug]/PlayerProfileTabs.tsx"
      provides: "No longer a >1000-line ceiling offender if it remains over the limit after 16-01/02"
    - path: "web/app/players/[slug]/page.tsx"
      provides: "No longer a >1000-line ceiling offender if it remains over the limit after 16-01/02"
  key_links:
    - from: "web/app/players/[slug]/PlayerProfileTabs.tsx"
      to: "web/app/players/[slug]/LiveAbProfilePanel.tsx"
      via: "player-profile tab composition"
      pattern: "LiveAbProfilePanel"
    - from: "web/lib/charting/live.ts"
      to: "web/app/charting/_components/ChartingEditor.tsx"
      via: "editor/live-state mutation helpers"
      pattern: "recordPitchInSnapshot"
---

<objective>
Audit all remaining source files in `web/` for the 1000-line ceiling, decompose any offenders that remain after Plans 16-01 and 16-02, and close Phase 16 with final verification.

Purpose: CODE-03 is a phase-level ceiling requirement, not a best-effort note. Even after the two named mega-files are split, Phase 16 is not complete until the rest of `web/` is audited and any remaining >1000-line source files are brought under the ceiling.

Output: a ceiling-clean `web/` source tree plus the final Phase 16 summary.
</objective>

<execution_context>
@/Users/traftonobrien/.claude/get-shit-done/workflows/execute-plan.md
@/Users/traftonobrien/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/16-code-decomposition/16-CONTEXT.md
@.planning/phases/16-code-decomposition/16-VALIDATION.md
@web/lib/charting/live.ts
@web/app/players/[slug]/PlayerProfileTabs.tsx
@web/app/players/[slug]/page.tsx

<interfaces>
Current known source-only ceiling offenders before execution:
- `web/app/charting/_components/ChartingEditor.tsx` — 2913
- `web/app/charting/insights/LiveAbInsightsExplorer.tsx` — 2863
- `web/lib/charting/live.ts` — 1514
- `web/app/players/[slug]/PlayerProfileTabs.tsx` — 1167
- `web/app/players/[slug]/page.tsx` — 1040

Source-only audit command (non-negotiable; exclude node_modules):
```bash
find web/app web/lib web/tests -type f \( -name '*.ts' -o -name '*.tsx' -o -name '*.js' -o -name '*.jsx' \) -print0 | xargs -0 wc -l | awk '$1 > 1000 && $2 != "total" { print }'
```

Phase posture:
- Preserve behavior while splitting remaining offenders.
- Use feature-local extraction rather than broad architectural rewrites.
- If Plans 16-01 and 16-02 already reduced some files below the ceiling, do not reopen them just for style reasons.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run the source-only ceiling audit and decompose any remaining offenders</name>
  <files>web/lib/charting/live.ts, web/app/players/[slug]/PlayerProfileTabs.tsx, web/app/players/[slug]/page.tsx, web/lib/charting/*, web/app/players/[slug]/*</files>
  <action>
Run the source-only audit command and use the result as the execution list for this plan.

Start from the current known likely offenders:
- `web/lib/charting/live.ts`
- `web/app/players/[slug]/PlayerProfileTabs.tsx`
- `web/app/players/[slug]/page.tsx`

For each file still over 1000 lines after Plans 16-01 and 16-02:
- decompose it using small, feature-local extractions,
- keep the behavior unchanged,
- keep each extracted module under 500 lines where practical,
- avoid using this plan for unrelated cleanup.

If a previously known offender is already below 1000 by the time this plan runs, leave it alone and move on to the next file reported by the audit.
  </action>
  <verify>
    <automated>cd /Users/traftonobrien/Desktop/pitch-tracker && ! find web/app web/lib web/tests -type f \( -name '*.ts' -o -name '*.tsx' -o -name '*.js' -o -name '*.jsx' \) -print0 | xargs -0 wc -l | awk '$1 > 1000 && $2 != "total" { print }' | grep -q .</automated>
  </verify>
  <done>The source-only audit reports no remaining files over 1000 lines.</done>
</task>

<task type="auto">
  <name>Task 2: Run final focused verification and phase-close audit</name>
  <files>.planning/phases/16-code-decomposition/16-03-SUMMARY.md</files>
  <action>
Run the final verification pass for the phase:
- source-only line-count audit across `web/app web/lib web/tests`
- focused targeted suites covering the decomposed charting and explorer/player-profile surfaces
- production build

Verification commands:
```bash
npm --prefix web test -- --run lib/charting/live.test.ts lib/charting/charting.test.ts lib/charting/setup.test.ts lib/charting/explorerState.test.ts lib/charting/playerComparison.test.ts lib/charting/pitcherComparison.test.ts lib/charting/playerProfile.test.ts
npm --prefix web run build
```

If `DATABASE_URL` is available and the repo can run the full suite cleanly, run `npm --prefix web test -- --run` as an extra final confidence pass. If not, document the pre-existing `auth.test.ts` env gate in the summary rather than treating it as a Phase 16 regression.

Write `16-03-SUMMARY.md` with:
- the final list of files decomposed in Phase 16,
- the final source-only ceiling audit result,
- targeted verification/build outcomes,
- any residual risks or follow-on cleanup intentionally deferred.
  </action>
  <verify>
    <automated>cd /Users/traftonobrien/Desktop/pitch-tracker && npm --prefix web test -- --run lib/charting/live.test.ts lib/charting/charting.test.ts lib/charting/setup.test.ts lib/charting/explorerState.test.ts lib/charting/playerComparison.test.ts lib/charting/pitcherComparison.test.ts lib/charting/playerProfile.test.ts && npm --prefix web run build</automated>
  </verify>
  <done>Phase 16 closes with no source files over 1000 lines, focused verification passing, and 16-03-SUMMARY.md written.</done>
</task>

</tasks>

<verification>
1. `find web/app web/lib web/tests ... | awk '$1 > 1000 ...'` — no output
2. `npm --prefix web test -- --run lib/charting/live.test.ts lib/charting/charting.test.ts lib/charting/setup.test.ts lib/charting/explorerState.test.ts lib/charting/playerComparison.test.ts lib/charting/pitcherComparison.test.ts lib/charting/playerProfile.test.ts` — targeted suites pass
3. `npm --prefix web run build` — build exits 0
4. `.planning/phases/16-code-decomposition/16-03-SUMMARY.md` exists
</verification>

<success_criteria>
- No file in `web/` exceeds 1000 lines at phase completion
- Remaining ceiling offenders are decomposed without reopening feature scope
- Final targeted verification and build both pass
- Phase 16 closeout summary is written
</success_criteria>

<output>
After completion, create `.planning/phases/16-code-decomposition/16-03-SUMMARY.md`
</output>
