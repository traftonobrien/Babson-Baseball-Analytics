---
phase: 16-code-decomposition
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - web/app/charting/_components/ChartingEditor.tsx
  - web/app/charting/_components/charting-editor/*
autonomous: true
requirements:
  - CODE-01

must_haves:
  truths:
    - "ChartingEditor.tsx becomes a thin composition file instead of a 2913-line monolith"
    - "Every extracted charting-editor module stays under 500 lines"
    - "The scorer workflow is preserved: header/top bar, zone workspace, action dock/history toggle, lineup/history flows, and optimistic save semantics still behave the same"
    - "No regression is introduced to pitch recording, PA closure, undo, lineup draft persistence, or revision-safe PATCH save flow"
  artifacts:
    - path: "web/app/charting/_components/ChartingEditor.tsx"
      provides: "Thin route entrypoint that composes extracted editor modules"
    - path: "web/app/charting/_components/charting-editor"
      provides: "Feature-local hooks, helpers, and workflow-panel components for the charting editor"
  key_links:
    - from: "web/app/charting/_components/ChartingEditor.tsx"
      to: "web/lib/charting/live.ts"
      via: "existing charting live-state helpers and snapshot mutation functions"
      pattern: "deriveChartingLiveState"
    - from: "web/app/charting/_components/ChartingEditor.tsx"
      to: "web/app/charting/_components/charting-editor/*"
      via: "workflow-panel composition and local state wiring"
      pattern: "charting-editor"
---

<objective>
Decompose `ChartingEditor.tsx` into focused, feature-local modules while preserving the current scoring flow and optimistic-save behavior.

Purpose: `ChartingEditor.tsx` is 2913 lines and currently mixes large workflow sections, local helper components, and editor state orchestration in one file. This plan turns it into a thin root composition file with co-located modules under the charting feature boundary.

Output: a decomposed charting editor with route-local modules under `web/app/charting/_components/charting-editor/`.
</objective>

<execution_context>
@/Users/traftonobrien/.claude/get-shit-done/workflows/execute-plan.md
@/Users/traftonobrien/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/16-code-decomposition/16-CONTEXT.md
@.planning/phases/16-code-decomposition/16-VALIDATION.md
@web/app/charting/_components/ChartingEditor.tsx
@web/lib/charting/live.ts

<interfaces>
<!-- Preserve these behaviors during extraction. -->

Current non-negotiables inside `ChartingEditor.tsx`:
- `lineupDraftStorageKey = charting-lineup-${initialSnapshot.game.id}` — lineup drafts must still survive hard refresh via sessionStorage.
- `clearPitchDraft()` resets pitch type, location, result, and pending velocity after a pitch is recorded or a PA closes.
- `queueSnapshotSave()` / `reloadLatestSnapshot()` preserve revision-safe PATCH behavior and conflict recovery; do not change the PATCH endpoint or revision semantics.
- The scorer flow remains anchored around the current workflow sections:
  - header + save/status controls
  - top bar / matchup / game-state / pitch-count sections
  - zone workspace
  - action dock with arsenal/result/history toggle
  - recent PA / history editing / lineup editing affordances

Known extraction candidates already inline in the file:
- `InPlayWizardContent`
- `ControlField`
- `SurfacePanel`
- `SectionHeading`
- `MetricPill`
- `StatTag`
- `StatusBadge`
- `SelectionButton`
- `PitchLocationGrid`
- helper/label/tone utilities near the bottom of the file
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract charting-editor primitives, constants, and workflow-panel components</name>
  <files>web/app/charting/_components/ChartingEditor.tsx, web/app/charting/_components/charting-editor/*</files>
  <action>
Create a feature-local folder for the editor decomposition, e.g. `web/app/charting/_components/charting-editor/`.

Extract the obvious inline presentational pieces and static helpers out of `ChartingEditor.tsx` first:
- workflow-panel UI sections (header/top bar pieces, zone workspace shell, action/history shells)
- inline presentational helpers such as `SurfacePanel`, `SectionHeading`, `StatusBadge`, `SelectionButton`, and `PitchLocationGrid`
- static constants / label / tone helpers that do not need to stay in the root file

Guidelines:
- Keep modules feature-local; do not turn this into a cross-app shared-components pass.
- Keep each extracted file under 500 lines.
- Preserve existing prop contracts and visible behavior; avoid changing copy, layout order, or control semantics unless necessary to preserve current behavior after extraction.
- Root `ChartingEditor.tsx` should get materially smaller after this task.
  </action>
  <verify>
    <automated>test $(wc -l < /Users/traftonobrien/Desktop/pitch-tracker/web/app/charting/_components/ChartingEditor.tsx) -lt 1000 && find /Users/traftonobrien/Desktop/pitch-tracker/web/app/charting/_components/charting-editor -type f \( -name '*.ts' -o -name '*.tsx' \) -exec sh -c 'n=$(wc -l < "$1"); [ "$n" -lt 500 ] || { echo "$1:$n"; exit 1; }' _ {} \;</automated>
  </verify>
  <done>ChartingEditor.tsx is a thinner composition file and extracted charting-editor modules exist with each module under 500 lines.</done>
</task>

<task type="auto">
  <name>Task 2: Extract editor-local state wiring where needed and preserve live charting behavior</name>
  <files>web/app/charting/_components/ChartingEditor.tsx, web/app/charting/_components/charting-editor/*</files>
  <action>
Reduce the remaining root-file size by extracting editor-local hooks/helpers only where the root composition still carries too much state orchestration.

Target seams:
- optimistic save / conflict-reload support
- history edit helpers
- lineup editor state
- baserunner draft handling
- in-play wizard support

Constraints:
- Keep the route API contract unchanged (`/api/charting/games/${id}` PATCH/GET usage must remain the same).
- Do not move charting-domain logic out of `web/lib/charting/live.ts` as part of this plan unless an extremely small helper extraction is required to keep the editor decomposition clean.
- Preserve behavior freeze: this is not a workflow redesign.

After extraction, run the focused charting verification commands and fix any decomposition regression before finishing the task.
  </action>
  <verify>
    <automated>cd /Users/traftonobrien/Desktop/pitch-tracker && npm --prefix web test -- --run lib/charting/live.test.ts lib/charting/charting.test.ts lib/charting/setup.test.ts && npm --prefix web run build</automated>
  </verify>
  <done>ChartingEditor decomposition preserves charting behavior, targeted charting tests pass, and the build succeeds.</done>
</task>

</tasks>

<verification>
1. `test $(wc -l < web/app/charting/_components/ChartingEditor.tsx) -lt 1000` — root file is below the ceiling
2. `find web/app/charting/_components/charting-editor -type f ...` — every extracted module is under 500 lines
3. `npm --prefix web test -- --run lib/charting/live.test.ts lib/charting/charting.test.ts lib/charting/setup.test.ts` — focused charting tests pass
4. `npm --prefix web run build` — build exits 0
</verification>

<success_criteria>
- `ChartingEditor.tsx` is no longer a mega-file and now acts as a thin root composition
- Extracted charting-editor modules are all under 500 lines
- The existing charting workflow and optimistic save behavior are preserved
- Targeted charting tests and build both pass
</success_criteria>

<output>
After completion, create `.planning/phases/16-code-decomposition/16-01-SUMMARY.md`
</output>
