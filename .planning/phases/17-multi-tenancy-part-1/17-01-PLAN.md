---
phase: 17-multi-tenancy-part-1
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - web/lib/teamConfig.ts
  - web/app/layout.tsx
  - web/app/HomeContent.tsx
  - web/app/login/page.tsx
  - web/app/player/[playerId]/report/page.tsx
  - web/app/mechanics/page.tsx
  - web/app/mechanics/faq/page.tsx
  - web/app/players/PlayersHubView.tsx
  - web/app/players/faq/page.tsx
  - web/app/trackman/faq/page.tsx
  - web/app/leaderboards/faq/page.tsx
  - web/app/team-stats/faq/page.tsx
  - web/app/charting/faq/page.tsx
  - web/app/charting/insights/_lib/helpers.ts
  - web/app/charting/insights/LiveAbInsightsExplorer.tsx
  - web/app/charting/insights/_components/empty-state.tsx
  - web/app/charting/_components/ChartingEditor.tsx
  - web/app/charting/_components/charting-editor/top-bar.tsx
  - web/app/charting/_components/charting-editor/lineup-editor-modal.tsx
  - web/lib/playerRegistry.ts
autonomous: true
requirements: [TEAM-01]

must_haves:
  truths:
    - "The string 'Babson' does not appear in any rendered page title, heading, label, or description — verified by grep on source files"
    - "A new deployment can set NEXT_PUBLIC_TEAM_NAME=<team> and all page titles, headings, and labels read from that value"
    - "getTeamConfig() returns the team name from env at runtime on the server"
    - "The build exits 0 after all replacements"
  artifacts:
    - path: "web/lib/teamConfig.ts"
      provides: "getTeamConfig() server helper + TEAM_NAME client constant"
      exports: ["getTeamConfig", "TEAM_NAME"]
    - path: "web/app/layout.tsx"
      provides: "Root metadata title reads from NEXT_PUBLIC_TEAM_NAME"
      contains: "NEXT_PUBLIC_TEAM_NAME"
  key_links:
    - from: "web/lib/teamConfig.ts"
      to: "process.env.NEXT_PUBLIC_TEAM_NAME"
      via: "env read at module load"
      pattern: "NEXT_PUBLIC_TEAM_NAME"
    - from: "web/app/layout.tsx"
      to: "web/lib/teamConfig.ts"
      via: "import TEAM_NAME"
      pattern: "import.*teamConfig"
---

<objective>
Audit every hardcoded "Babson" string in the web/ source tree, replace each one with a value sourced from a single configurable constant, and verify the build passes clean.

Purpose: Any D3 team deploying this platform must be able to set one env var and have their team name appear everywhere — no code changes required.
Output: web/lib/teamConfig.ts + all call-site replacements + zero "Babson" literals in rendered page strings.
</objective>

<execution_context>
@/Users/traftonobrien/.claude/get-shit-done/workflows/execute-plan.md
@/Users/traftonobrien/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-code-decomposition/16-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create web/lib/teamConfig.ts and audit all Babson occurrences</name>
  <files>web/lib/teamConfig.ts</files>
  <action>
First run the audit grep to confirm the full scope:

```
grep -rn "Babson" web/app web/lib \
  --include="*.ts" --include="*.tsx" \
  | grep -v "\.next/" | grep -v "node_modules"
```

Then create web/lib/teamConfig.ts with the following content exactly:

```typescript
/**
 * Team identity configuration.
 *
 * NEXT_PUBLIC_TEAM_NAME is set in Vercel environment variables.
 * It is read at build time for client components and at runtime for server components.
 *
 * Default: "Babson" — so existing deployments are unchanged until overridden.
 */
export const TEAM_NAME: string =
  process.env.NEXT_PUBLIC_TEAM_NAME ?? "Babson";

/**
 * Server-side helper that returns the active team config.
 * Extend this object as Phase 18 adds logo, colors, and branding fields.
 */
export function getTeamConfig() {
  return {
    name: TEAM_NAME,
  };
}
```

This file is the single source of truth. All call sites import from here — never from process.env directly.

Note: NEXT_PUBLIC_TEAM_NAME is the right prefix because Next.js inlines it into client bundles at build time, and server code can also read it via process.env at runtime. One variable, both runtimes.
  </action>
  <verify>
    <automated>test -f /Users/traftonobrien/Desktop/pitch-tracker/web/lib/teamConfig.ts && echo "EXISTS"</automated>
  </verify>
  <done>teamConfig.ts exists and exports TEAM_NAME and getTeamConfig</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Replace all rendered "Babson" literals with TEAM_NAME or getTeamConfig().name</name>
  <files>
    web/app/layout.tsx,
    web/app/HomeContent.tsx,
    web/app/login/page.tsx,
    web/app/player/[playerId]/report/page.tsx,
    web/app/mechanics/page.tsx,
    web/app/mechanics/faq/page.tsx,
    web/app/players/PlayersHubView.tsx,
    web/app/players/faq/page.tsx,
    web/app/trackman/faq/page.tsx,
    web/app/leaderboards/faq/page.tsx,
    web/app/team-stats/faq/page.tsx,
    web/app/charting/faq/page.tsx,
    web/app/charting/insights/_lib/helpers.ts,
    web/app/charting/insights/LiveAbInsightsExplorer.tsx,
    web/app/charting/insights/_components/empty-state.tsx,
    web/app/charting/_components/ChartingEditor.tsx,
    web/app/charting/_components/charting-editor/top-bar.tsx,
    web/app/charting/_components/charting-editor/lineup-editor-modal.tsx,
    web/lib/playerRegistry.ts
  </files>
  <action>
For each file listed, add `import { TEAM_NAME } from "@/lib/teamConfig"` (or `getTeamConfig` for server components) and replace every rendered "Babson" string. Strategy per file:

**web/app/layout.tsx**
- `title: "Babson Baseball Pitching Portal"` → `title: \`${TEAM_NAME} Baseball Pitching Portal\``
- Import TEAM_NAME at top of file (client-safe: NEXT_PUBLIC_ prefix)

**web/app/HomeContent.tsx**
- Three occurrences: heading text "Babson Baseball", alt text "Babson Baseball", subtitle "Babson Baseball Data Insights Portal", footer "Home of the Babson Beavers"
- Replace each with `{TEAM_NAME}` interpolation in JSX or template literal
- The footer "Home of the Babson Beavers" — use: `Home of the {TEAM_NAME} Beavers` (mascot is not parameterized in Phase 17; leave "Beavers" literal — only "Babson" is replaced)
- Import TEAM_NAME

**web/app/login/page.tsx**
- `"Babson Baseball Pitching Portal"` → template literal with TEAM_NAME
- Import TEAM_NAME

**web/app/player/[playerId]/report/page.tsx**
- `"Babson Baseball Pitching Portal"` footer span → template literal with TEAM_NAME
- Import TEAM_NAME

**web/app/mechanics/page.tsx**
- `title: "Mechanics Hub — Babson Baseball"` → template literal with TEAM_NAME
- Import TEAM_NAME

**web/app/mechanics/faq/page.tsx**, **web/app/players/faq/page.tsx**, **web/app/trackman/faq/page.tsx**, **web/app/leaderboards/faq/page.tsx**, **web/app/team-stats/faq/page.tsx**, **web/app/charting/faq/page.tsx**
- All have the pattern `title: "... — Babson Baseball"`
- Replace with template literal using TEAM_NAME
- Import TEAM_NAME in each

**web/app/players/PlayersHubView.tsx**
- `player.team || "Babson Baseball"` → `player.team || \`${TEAM_NAME} Baseball\``
- `description="Start with the full Babson roster..."` → `description={\`Start with the full ${TEAM_NAME} roster...\`}`
- Import TEAM_NAME

**web/app/charting/insights/_lib/helpers.ts**
- Two occurrences of `"A Babson take on the Savant..."` description strings
- Replace "Babson" in each string with `${TEAM_NAME}` — convert to template literals
- Import TEAM_NAME

**web/app/charting/insights/LiveAbInsightsExplorer.tsx**
- Two tooltip strings on lines ~747-748 containing "Babson charting data"
- Replace "Babson" with `${TEAM_NAME}` in template literals
- Import TEAM_NAME

**web/app/charting/insights/_components/empty-state.tsx**
- Two strings containing "Babson pitcher data" and "Babson data"
- Replace "Babson" with `${TEAM_NAME}` in template literals
- Import TEAM_NAME

**web/app/charting/_components/ChartingEditor.tsx**
- Line 175: `snapshot.game.ourTeamLabel?.trim() || "Babson"` → `snapshot.game.ourTeamLabel?.trim() || TEAM_NAME`
- Line 753: `\`Babson set as ${newSide}\`` → `\`${TEAM_NAME} set as ${newSide}\``
- Import TEAM_NAME

**web/app/charting/_components/charting-editor/top-bar.tsx**
- Line 137: `"Babson pitcher"` → `` `${TEAM_NAME} pitcher` ``
- Line 158: `"Babson hitter"` → `` `${TEAM_NAME} hitter` ``
- Import TEAM_NAME

**web/app/charting/_components/charting-editor/lineup-editor-modal.tsx**
- `["our", ourTeamLabel, "Babson hitter"]` → `["our", ourTeamLabel, \`${TEAM_NAME} hitter\`]`
- Import TEAM_NAME

**web/lib/playerRegistry.ts**
- Line 44: `entry.team ?? entry.school ?? "Babson"` → `entry.team ?? entry.school ?? TEAM_NAME`
- Line 106: `pitcherEntry?.team ?? "Babson"` → `pitcherEntry?.team ?? TEAM_NAME`
- Import TEAM_NAME

**DO NOT change:**
- `web/lib/teamBranding.ts` — the `name: "Babson"` entry in TEAM_BRAND_DIRECTORY is for opponent brand lookup (it is the opponent team name string, not our team name). This is NOT a hardcoded rendered team name — it is a data entry. Leave it untouched.
- `web/lib/charting/types.ts` — JSDoc comments only, not rendered strings. Leave as-is.
- `web/lib/charting/fixtures.ts` — Test fixture, not rendered. Leave as-is.
- `web/app/charting/_components/ChartingCreateForm.tsx` — `babsonVenueSide`, `babsonStartingPitcher` are internal variable names and form field state, not rendered strings. The string "Babson" does not appear in any label, placeholder, or rendered text in this file — it is only in variable names. Leave as-is.
- `web/app/api/**` — Comments and internal type names only (e.g., `BabsonPitcherRow` type name). These are not rendered strings. Leave as-is.
- `web/app/components/Header.tsx` — `alt="Babson College"` is the img alt text for the school logo. Leave as-is in Phase 17 (logo and branding parameterization is Phase 18).
- `web/app/trackman/player/[slug]/MLBCompsPanel.tsx` — JSX comment only, not rendered text. Leave as-is.
  </action>
  <verify>
    <automated>grep -rn "Babson" /Users/traftonobrien/Desktop/pitch-tracker/web/app /Users/traftonobrien/Desktop/pitch-tracker/web/lib --include="*.ts" --include="*.tsx" | grep -v "\.next/" | grep -v "node_modules" | grep -v "teamBranding" | grep -v "fixtures.ts" | grep -v "ChartingCreateForm" | grep -v "types.ts" | grep -v "Header.tsx" | grep -v "MLBCompsPanel" | grep -v "api/" | grep -v "// " | grep -v "/\*" | grep -v "\*"</automated>
  </verify>
  <done>
    - grep on source files returns zero remaining "Babson" literals in rendered strings (comments and intentional data entries excluded per the DO NOT change list above)
    - Every replaced file imports from @/lib/teamConfig
    - npm --prefix web run build exits 0
  </done>
</task>

</tasks>

<verification>
After both tasks complete:

1. Run the audit grep to confirm zero rendered Babson literals:
   `grep -rn "Babson" web/app web/lib --include="*.ts" --include="*.tsx" | grep -v "\.next/" | grep -v "// " | grep -v "\*"`
   Any remaining hits should be ONLY in: teamBranding.ts (data directory), fixtures.ts, types.ts (JSDoc), ChartingCreateForm.tsx (variable names), Header.tsx (img alt), MLBCompsPanel.tsx (comment), api/ (type names).

2. Build must pass:
   `npm --prefix web run build`

3. Confirm teamConfig.ts exists and is the sole import source for TEAM_NAME across all modified files.
</verification>

<success_criteria>
- NEXT_PUBLIC_TEAM_NAME=Acme set in .env.local renders "Acme" in all page titles and headings
- grep audit confirms zero rendered "Babson" strings remain in modified source files
- npm --prefix web run build exits 0
- TEAM-01 satisfied: all hardcoded team name references read from configurable source
</success_criteria>

<output>
After completion, create `.planning/phases/17-multi-tenancy-part-1/17-01-SUMMARY.md`
</output>
