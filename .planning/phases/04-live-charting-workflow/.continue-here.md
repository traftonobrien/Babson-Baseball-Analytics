---
phase: 04-live-charting-workflow
task: 1
total_tasks: 1
status: in_progress
last_updated: 2026-03-10T16:09:48.498Z
---

<current_state>
Paused at a clean pre-edit checkpoint for a new web charting enhancement: add `2-1` and `Bunt` starting states to the charting editor. No files were changed yet for this feature. The current worktree already contains unrelated charting hub, leaderboard, innings, and PDF changes from earlier work; do not revert them while resuming this task.
</current_state>

<completed_work>

- Confirmed the web live-state math already supports seeded counts via `derivePAPitchProgress(pitches, seedBalls?, seedStrikes?)` in `web/lib/charting/live.ts`.
- Confirmed the web editor currently has no preset UI or next-PA seed state in `web/app/charting/_components/ChartingEditor.tsx`.
- Confirmed first-pitch recording always creates PAs with `buntContext: false` and seeds `ballsBefore` / `strikesBefore` from the current live state, which means every new PA effectively starts from `0-0`.
- Matched intended behavior against the iOS implementation:
  - `LiveABCountPreset` supports `0-0`, `2-1`, and `Bunt`.
  - Bunt mode limits actions to `ball`, `foul`, and `in_play`.
  - A bunt-mode `foul` is normalized to `bunt_foul`.
  - Committed bunt reps mark `buntContext = true`.
- Found an additional web bug that should be fixed in the same pass: `applyPitchResult` in `web/lib/charting/live.ts` does not handle `bunt_foul` at all, so live count derivation would drift even after the UI starts sending it.
</completed_work>

<remaining_work>

- Add a small preset model for the web charting editor, likely `0-0 | 2-1 | bunt`, with helpers for starting balls, starting strikes, and bunt mode.
- Update `web/lib/charting/live.ts` so the next PA can be seeded from the selected preset when there is no open PA yet.
- Update `recordPitchInSnapshot(...)` to:
  - seed the first pitch with preset count values,
  - normalize bunt-mode `foul` to `bunt_foul`,
  - set `openPA.buntContext = true` for bunt reps or `bunt_foul`.
- Fix `applyPitchResult(...)` so `bunt_foul` behaves like a bunt foul:
  - add a strike if below two strikes,
  - on two strikes, promote to strikeout closure.
- Update `web/app/charting/_components/ChartingEditor.tsx` to:
  - expose preset controls near the current at-bat / live count surface,
  - show the preset count before the first pitch of a PA,
  - restrict result buttons to bunt-safe actions in bunt mode,
  - use bunt-aware labels like `Foul Bunt` / `Fair Bunt`,
  - reset the local preset back to `0-0` after the first pitch is committed.
- Add targeted tests in `web/lib/charting/live.test.ts` for:
  - first pitch in `2-1` mode storing `ballsBefore: 2`, `strikesBefore: 1`,
  - bunt mode converting `foul` to `bunt_foul`,
  - bunt mode setting `buntContext`,
  - `bunt_foul` with two strikes closing as strikeout.
- Run targeted tests and a web build after the patch.
</remaining_work>

<decisions_made>

- Keep the first implementation local-state-first in the web editor instead of changing database/schema shape. The preset only needs to exist until the first pitch is committed, because the committed pitch already stores `ballsBefore` and `strikesBefore`.
- Mirror the iOS behavior closely so the charting rules stay aligned across clients.
- Fix the latent `bunt_foul` live-state bug in the same change set. Shipping preset UI without that fix would create inconsistent counts immediately after a bunt foul.
</decisions_made>

<blockers>
- No hard blocker. The only notable risk is the dirty worktree: there are many unrelated modified/untracked charting files already present, so resume work should patch only the web live-charting files for this feature.
</blockers>

<context>
This is best resumed as a narrow web live-charting patch, not a broader charting-system refactor. The cleanest path is to keep persistence unchanged, carry preset state in `ChartingEditor.tsx`, thread that state into `recordPitchInSnapshot(...)`, and let committed pitch rows become the durable source of truth. iOS already gives the exact behavioral contract to mirror.

Relevant files to reopen first:
- `web/lib/charting/live.ts`
- `web/lib/charting/live.test.ts`
- `web/app/charting/_components/ChartingEditor.tsx`
- `ios/ChartingApp/ChartingApp/Models/Models.swift`
- `ios/ChartingApp/ChartingApp/Models/ChartingState.swift`
</context>

<next_action>
Start with `web/lib/charting/live.ts`: add preset seed types/options, normalize bunt-mode fouls to `bunt_foul`, and teach `applyPitchResult(...)` to handle `bunt_foul`. Then wire the editor UI/state in `web/app/charting/_components/ChartingEditor.tsx`, add tests in `web/lib/charting/live.test.ts`, and run targeted tests plus `npm --prefix web run build`.
</next_action>
