---
phase: 17.5-supabase-migration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - web/db/index.ts
  - web/package.json
  - web/.env.local
autonomous: true
requirements:
  - TEAM-02
  - OPS-01

must_haves:
  truths:
    - "All charting API routes read and write through the Supabase Postgres connection — no neon-http in production code"
    - "npm run build passes with the new driver and no neon-http import remains in web/db/index.ts"
    - "Existing charting data (games, pitcher segments, plate appearances, pitches, lineups) is fully accessible from Supabase"
  artifacts:
    - path: "web/db/index.ts"
      provides: "Drizzle db instance backed by Supabase Postgres via postgres-js"
      exports: ["db"]
      contains: "drizzle-orm/postgres-js"
  key_links:
    - from: "web/db/index.ts"
      to: "Supabase Postgres (ojrkhiatugtrwavyeakk.supabase.co)"
      via: "postgres() connection string"
      pattern: "postgres\\(process\\.env\\.DATABASE_URL"
---

<objective>
Swap the database driver from neon-http to Supabase Postgres. Replace `@neondatabase/serverless` + `drizzle-orm/neon-http` with the `postgres` npm package + `drizzle-orm/postgres-js`, pointed at the Supabase project. Migrate all existing charting data from Neon to Supabase so the app is fully cutover.

Purpose: Eliminate the Neon dependency. All subsequent plans (RLS policies, storage) depend on the app running against Supabase.

Output: Updated `web/db/index.ts`, updated `web/package.json`, updated `DATABASE_URL` pointing to Supabase.
</objective>

<execution_context>
@/Users/traftonobrien/.claude/get-shit-done/workflows/execute-plan.md
@/Users/traftonobrien/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17.5-supabase-migration/17.5-CONTEXT.md

<interfaces>
<!-- Current driver — to be replaced entirely -->
From web/db/index.ts (CURRENT — will be replaced):
```typescript
import { neon } from "@neondatabase/serverless";
import { drizzle } from "drizzle-orm/neon-http";

const sql = neon(process.env.DATABASE_URL!);
export const db = drizzle(sql);
```

<!-- New driver — target implementation -->
Target web/db/index.ts:
```typescript
import { drizzle } from "drizzle-orm/postgres-js";
import postgres from "postgres";

const client = postgres(process.env.DATABASE_URL!);
export const db = drizzle(client);
```

Supabase project: ojrkhiatugtrwavyeakk
Supabase DB connection string format: postgresql://postgres.[project-ref]:[password]@aws-0-us-east-1.pooler.supabase.com:6543/postgres
(Use the "Transaction" pooler mode connection string from Supabase Dashboard > Settings > Database for serverless/edge compatibility)

Important: drizzle-orm/neon-http does NOT support db.transaction() — the codebase correctly uses db.batch() already (see comment in web/app/api/charting/games/[id]/route.ts:282). The postgres-js adapter DOES support db.transaction() but db.batch() also works — no query changes required.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Install postgres-js and remove neon dependency</name>
  <files>web/package.json</files>
  <action>
    In the web/ directory:

    1. Install postgres-js: `npm install postgres`
    2. Remove neon: `npm uninstall @neondatabase/serverless`
    3. Verify drizzle-orm and drizzle-kit are still present (they ship their own adapter, no version change needed)

    After package changes, confirm `web/package.json` contains `"postgres"` in dependencies and does NOT contain `"@neondatabase/serverless"`.

    Note: drizzle-orm/postgres-js is included in the existing drizzle-orm package — no separate install needed.
  </action>
  <verify>
    <automated>grep -v "@neondatabase" /Users/traftonobrien/Desktop/pitch-tracker/web/package.json && grep '"postgres"' /Users/traftonobrien/Desktop/pitch-tracker/web/package.json</automated>
  </verify>
  <done>package.json has "postgres" in dependencies; "@neondatabase/serverless" is absent</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Swap web/db/index.ts to postgres-js + Drizzle</name>
  <files>web/db/index.ts</files>
  <action>
    Replace the entire contents of `web/db/index.ts` with:

    ```typescript
    import { drizzle } from "drizzle-orm/postgres-js";
    import postgres from "postgres";

    const client = postgres(process.env.DATABASE_URL!);
    export const db = drizzle(client);
    ```

    DATABASE_URL must be the Supabase "Transaction pooler" connection string from:
    Supabase Dashboard → Project ojrkhiatugtrwavyeakk → Settings → Database → Connection string → Transaction (port 6543).

    Update `web/.env.local` to set DATABASE_URL to the Supabase transaction pooler URL. The env var name stays the same — only the value changes. Do NOT commit `.env.local`.

    Also update `web/drizzle.config.ts` — it already reads `DATABASE_URL` so no structural change needed there; just confirm it still points to the right env var.

    IMPORTANT: The drizzle.config.ts uses `dotenv` and `process.env.DATABASE_URL` — this still works with the new driver.
  </action>
  <verify>
    <automated>grep "drizzle-orm/postgres-js" /Users/traftonobrien/Desktop/pitch-tracker/web/db/index.ts && ! grep "neon" /Users/traftonobrien/Desktop/pitch-tracker/web/db/index.ts</automated>
  </verify>
  <done>web/db/index.ts imports from drizzle-orm/postgres-js; no neon import exists; DATABASE_URL in .env.local points to Supabase</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Migrate Neon data to Supabase and verify build</name>
  <files>web/db/index.ts</files>
  <action>
    Data migration strategy (choose the path that applies):

    **Option A — Supabase import from Neon (preferred):**
    Use the Supabase Dashboard → Database → Migrations → "Import from external database" wizard, providing the Neon DATABASE_URL. This imports schema + all table data.

    **Option B — pg_dump / psql:**
    ```bash
    # Dump from Neon
    pg_dump "$NEON_DATABASE_URL" --no-owner --no-acl -Fc -f neon_dump.dump

    # Restore to Supabase
    pg_restore --dbname="$SUPABASE_DATABASE_URL" --no-owner --no-acl neon_dump.dump
    ```

    After migration, apply the Drizzle migration history:
    - All 9 migration files in `web/drizzle/` (0000 through 0008) represent the schema state already in Neon.
    - If using pg_dump restore, the schema migrates with the data — no separate `drizzle-kit migrate` needed.
    - If schema is empty on Supabase side, run `npm --prefix web run db:push` to push schema without running individual migrations.

    Verify the migration succeeded by running the build — it will fail with a connection error if DATABASE_URL is wrong or tables are missing:
    ```bash
    npm --prefix web run build
    ```

    The build must exit 0. The existing charting API routes import `db` from `web/db/index.ts` — if the connection is wrong, TypeScript compilation will still pass but runtime would fail. Build exit 0 + no import errors is the acceptance signal.

    Also run the existing test suite:
    ```bash
    npm --prefix web test -- --run 2>&1 | tail -20
    ```

    Note: `auth.test.ts` requires DATABASE_URL at runtime — it will now use the Supabase URL. All other tests are pure unit tests and do not hit the DB.
  </action>
  <verify>
    <automated>cd /Users/traftonobrien/Desktop/pitch-tracker/web && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>npm run build exits 0 with no neon-http import errors; test suite passes (auth.test.ts may be skipped if no DATABASE_URL in CI env)</done>
</task>

</tasks>

<verification>
After all three tasks:
1. `grep -r "neon" web/db/index.ts` returns nothing
2. `grep -r "@neondatabase" web/ --include="*.ts" --include="*.tsx" | grep -v node_modules` returns nothing
3. `npm --prefix web run build` exits 0
4. `npm --prefix web test -- --run` passes (excluding pre-existing auth.test.ts DATABASE_URL issue)
</verification>

<success_criteria>
- web/db/index.ts uses drizzle-orm/postgres-js — no neon import
- @neondatabase/serverless is not in package.json dependencies
- DATABASE_URL in .env.local points to Supabase transaction pooler
- Existing charting data is accessible from Supabase
- npm run build exits 0
</success_criteria>

<output>
After completion, create `.planning/phases/17.5-supabase-migration/17.5-01-SUMMARY.md`
</output>
