---
phase: 14-completion
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - web/lib/charting/playerProfile.ts
  - web/lib/charting/playerProfile.test.ts
  - web/app/players/[slug]/LiveAbProfilePanel.tsx
  - web/app/players/[slug]/PlayerProfileTabs.tsx
  - .planning/phases/12.1-live-ab-player-profile-integration/12.1-03-SUMMARY.md
autonomous: true
requirements: [DONE-01]

must_haves:
  truths:
    - "Two-way players show both pitcher section and hitter section with a two-way indicator below."
    - "Hitter-only players show only the hitter section with no broken pitcher section."
    - "Pitcher-only players show only the pitcher section with no broken hitter section."
    - "No-data players see the empty-state message — no blank panel or null crash."
    - "Full test suite passes (369 tests in 34 files) and build is clean."
    - "12.1-03-SUMMARY.md exists documenting the completed work."
  artifacts:
    - path: "web/lib/charting/playerProfile.test.ts"
      provides: "Regression coverage for mixed-role and empty-state behavior"
      exports: ["it(two-way)", "it(hitter-only)", "it(pitcher-only)", "it(no-data)"]
    - path: "web/app/players/[slug]/LiveAbProfilePanel.tsx"
      provides: "Polished Live AB panel handling all four role states"
    - path: ".planning/phases/12.1-live-ab-player-profile-integration/12.1-03-SUMMARY.md"
      provides: "Phase 12.1 completion record"
  key_links:
    - from: "web/app/players/[slug]/LiveAbProfilePanel.tsx"
      to: "web/lib/charting/playerProfile.ts"
      via: "ChartingPlayerProfile contract (profile.availableRoles, profile.pitcher, profile.hitter)"
      pattern: "availableRoles"
    - from: "web/app/players/[slug]/LiveAbProfilePanel.tsx"
      to: "web/app/charting/games/[id]"
      via: "recent-session links"
      pattern: "/charting/games/"
---

<objective>
Complete Phase 12.1-03 — run a focused review-and-polish pass on the existing Live AB player profile implementation, confirm all four role states are handled correctly, and close the phase with a SUMMARY.md.

Purpose: The implementation is largely complete from the March 19 synthesis expansion. This plan validates correctness, fixes any genuine gaps found during review, runs the full validation gate, and writes the completion record so Phase 12.1 can be marked done.

Output: 12.1-03-SUMMARY.md, passing test suite, clean build.
</objective>

<execution_context>
@/Users/traftonobrien/.claude/get-shit-done/workflows/execute-plan.md
@/Users/traftonobrien/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12.1-live-ab-player-profile-integration/12.1-02-SUMMARY.md
@web/lib/charting/playerProfile.ts
@web/lib/charting/playerProfile.test.ts
@web/app/players/[slug]/LiveAbProfilePanel.tsx
@web/app/players/[slug]/PlayerProfileTabs.tsx

<interfaces>
<!-- Key contracts the executor needs. DO NOT deviate from these. -->

From web/lib/charting/playerProfile.ts (ChartingPlayerProfile contract):
The profile object passed to LiveAbProfilePanel has:
  profile.availableRoles: ("pitcher" | "hitter")[]
  profile.defaultRole: "pitcher" | "hitter"
  profile.pitcher: ChartingPitcherProfile | null
  profile.hitter: ChartingHitterProfile | null

Role detection rules (non-negotiable):
  pitcher-only:  profile.pitcher !== null && profile.hitter === null
  hitter-only:   profile.pitcher === null && profile.hitter !== null
  two-way:       profile.pitcher !== null && profile.hitter !== null
  no-data:       profile.pitcher === null && profile.hitter === null

Synthesis helpers (already implemented — do NOT rewrite):
  derivePitchingSynthesis(pitchStats: PitchTypeStats[]): string[]
  deriveHittingSynthesis(stats: AggregatedHitterStats): string[]
  deriveHittingSynthesis guards on totalPAs < 15 — this is correct, do not remove.

Two-way indicator (current implementation at ~line 817):
  {profile.availableRoles.length > 1 ? (
    <div className="flex items-center gap-2 text-xs text-zinc-500">
      <BarChart3 className="h-4 w-4" />
      This player has both pitcher and hitter Charting data in the charting system.
    </div>
  ) : null}

No-data guard (current implementation at ~line 497):
  if (!pitcher && !hitter) returns LeaderboardPanel with empty-state message.

From web/lib/charting/playerProfile.test.ts:
  Existing tests (5 total): pitcher-only, hitter alias merge, two-way, no-data, directory build.
  All 5 pass. auth.test.ts fails locally due to missing DATABASE_URL — pre-existing, ignore.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Review and polish all four role states in LiveAbProfilePanel</name>
  <files>web/app/players/[slug]/LiveAbProfilePanel.tsx, web/lib/charting/playerProfile.test.ts</files>
  <behavior>
    - Test: pitcher-only player renders pitcher section, hitter section is absent (null guard works)
    - Test: hitter-only player renders hitter section, pitcher section is absent
    - Test: two-way player renders both sections plus the two-way indicator message
    - Test: no-data player renders the empty-state panel (not null/crash)
    - Test: deriveHittingSynthesis returns empty array when totalPAs < 15 (sample-size guard)
  </behavior>
  <action>
    Read LiveAbProfilePanel.tsx fully before making any changes.

    Step 1 — Audit. For each of the four role states, confirm:
    - The no-data guard at the top returns the empty-state LeaderboardPanel when both pitcher and hitter are null. Check the message is clear: "No charting data has been recorded for this player yet."
    - The pitcher section is wrapped in {pitcher ? (...) : null} — no unconditional references to pitcher fields.
    - The hitter section is wrapped in {hitter ? (...) : null} — no unconditional references to hitter fields.
    - The two-way indicator ({profile.availableRoles.length > 1 ? ... : null}) renders after the hitter section.
    - Recent-session links inside pitcher and hitter sections use /charting/games/ prefix (not /player/ or /charting/insights/).
    - deriveHittingSynthesis is called as deriveHittingSynthesis(hitter.stats) — hitter is guaranteed non-null at that call site.
    - The totalPAs < 15 guard inside deriveHittingSynthesis is still present and not removed.

    Step 2 — Fix only genuine gaps found in Step 1. If nothing is broken, make no code changes to the panel. Do not add features. Do not rename the "Charting" tab to "Live AB". Do not add hittingSeasonStats.

    Step 3 — Add or strengthen playerProfile.test.ts if any of the four role-state unit tests are missing or weak. The 5 existing tests cover the main scenarios; only add tests for gaps found in Step 1. Tests live in web/lib/charting/playerProfile.test.ts and test the data contract (playerProfile.ts), not the React component.

    Run after any change:
    npm --prefix web test -- --run lib/charting/playerProfile
  </action>
  <verify>
    <automated>npm --prefix web test -- --run lib/charting/playerProfile</automated>
  </verify>
  <done>All playerProfile tests pass. LiveAbProfilePanel correctly handles pitcher-only, hitter-only, two-way, and no-data states with no null crashes or missing sections. No regressions introduced.</done>
</task>

<task type="auto">
  <name>Task 2: Run full validation gate and create 12.1-03-SUMMARY.md</name>
  <files>.planning/phases/12.1-live-ab-player-profile-integration/12.1-03-SUMMARY.md</files>
  <action>
    Step 1 — Run the full test suite:
    npm --prefix web test -- --run

    Expected: 369 tests pass in 34 files. auth.test.ts (1 file) fails with "neon() called without DATABASE_URL" — this is a pre-existing local environment issue, NOT a regression. If any file other than auth.test.ts fails, stop and diagnose before proceeding.

    Step 2 — Run the build:
    npm --prefix web run build

    If build fails, diagnose and fix before proceeding.

    Step 3 — Write .planning/phases/12.1-live-ab-player-profile-integration/12.1-03-SUMMARY.md using the standard summary template. Include:
    - What was implemented (all four role states in LiveAbProfilePanel, synthesis helpers, playerProfile.ts contract)
    - Files modified (list with one-line purpose each)
    - Test coverage (5 tests in playerProfile.test.ts covering pitcher-only, hitter alias, two-way, no-data, directory)
    - Validation results (test count, build status)
    - Key decisions (hitter seasonStats intentionally excluded; "Charting" tab label kept; UAT route is /players/[slug]?tab=charting)
    - Phase 12.1 is now complete — all three plans done
  </action>
  <verify>
    <automated>npm --prefix web run build</automated>
  </verify>
  <done>Full test suite passes (369/369 in non-auth files), build is clean, and 12.1-03-SUMMARY.md exists with complete content.</done>
</task>

</tasks>

<verification>
1. `npm --prefix web test -- --run lib/charting/playerProfile` — all playerProfile tests pass
2. `npm --prefix web test -- --run` — 369 tests pass (auth.test.ts failure expected, not a regression)
3. `npm --prefix web run build` — clean build, no TypeScript errors
4. `.planning/phases/12.1-live-ab-player-profile-integration/12.1-03-SUMMARY.md` exists with content
</verification>

<success_criteria>
- LiveAbProfilePanel handles pitcher-only, hitter-only, two-way, and no-data states without null crashes or blank sections
- All four role-state behaviors are covered by tests in playerProfile.test.ts
- Full test suite (369 non-auth tests) and production build both pass
- 12.1-03-SUMMARY.md written — Phase 12.1 marked complete
</success_criteria>

<output>
After completion, create `.planning/phases/14-completion/14-01-SUMMARY.md`
</output>
