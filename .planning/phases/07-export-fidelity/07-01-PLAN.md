---
phase: 07-export-fidelity
plan: 01
type: execute
wave: 1
depends_on: [06-portal-charting-surfaces]
files_modified:
  - web/lib/charting/snapshot.ts
  - web/lib/charting/export.ts
  - web/app/api/charting/games/[id]/export/route.ts
  - web/app/charting/games/[id]/page.tsx
autonomous: true
user_setup: []
must_haves:
  truths:
    - Staff can download a structured CSV directly from the portal game detail view.
  artifacts:
    - web/app/api/charting/games/[id]/export/route.ts
    - web/lib/charting/export.ts
  key_links:
    - /charting/games/[id] -> /api/charting/games/[id]/export
---

# Phase 7 Plan 01: Build Normalized CSV Export

<objective>
Build the first export surface for charted games by generating a normalized CSV from the shared portal snapshot and exposing it directly from the game detail page.
</objective>

## Objective
- **What:** Add a normalized CSV export for charted games.
- **Why:** Staff needs spreadsheet-friendly output before the paper-style PDF exists.
- **Output:** A download route plus portal button backed by shared export utilities and tests.

## Context
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-export-fidelity/07-CONTEXT.md

## Tasks
<task type="code">
  <name>Build shared chart export utilities</name>
  <files>web/lib/charting/snapshot.ts, web/lib/charting/export.ts</files>
  <action>Create a shared snapshot loader and normalize chart data into CSV rows that stay aligned with the portal detail view.</action>
  <verify>`npm --prefix web test -- export.test.ts` passes.</verify>
  <done>CSV serialization is reusable and fixture-backed.</done>
</task>

<task type="code">
  <name>Expose export route and download entry point</name>
  <files>web/app/api/charting/games/[id]/export/route.ts, web/app/charting/games/[id]/page.tsx</files>
  <action>Add the export API route and a direct download button on the game detail page.</action>
  <verify>The page links to a working CSV response for a synced game.</verify>
  <done>Staff can trigger CSV export from the portal detail view.</done>
</task>

## Verification
- `npm --prefix web test -- export.test.ts`

## Success Criteria
- [x] Shared export code reads the same chart snapshot as the portal detail surface
- [x] The portal game detail page exposes a working CSV download
