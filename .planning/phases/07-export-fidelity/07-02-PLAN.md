---
phase: 07-export-fidelity
plan: 02
type: execute
wave: 1
depends_on: [07-01]
files_modified:
  - web/app/api/charting/games/[id]/export-pdf/route.ts
  - web/lib/charting/pdf.ts
  - web/app/charting/games/[id]/page.tsx
autonomous: true
user_setup: []
must_haves:
  truths:
    - Staff can download a paper-style PDF for a charted game from the portal.
  artifacts:
    - web/lib/charting/pdf.ts
  key_links:
    - shared chart snapshot -> PDF renderer
---

# Phase 7 Plan 02: Build Paper-Style PDF Renderer

<objective>
Add a staff-usable PDF export that approximates the current paper chart while staying faithful to the same underlying chart snapshot used by the CSV export and portal review surfaces.
</objective>

## Objective
- **What:** Implement the paper-style PDF export surface for charted games.
- **Why:** CSV solves spreadsheet workflows, but staff still needs a recognizable chart-style document for review and sharing.
- **Output:** A PDF generation route, renderer, and portal download entry point.

## Context
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-export-fidelity/07-CONTEXT.md

## Tasks
<task type="code">
  <name>Design the export view model and PDF renderer</name>
  <files>web/lib/charting/pdf.ts</files>
  <action>Map the shared chart snapshot into a paper-style layout model that includes chart header fields, pitch log structure, and per-pitcher summary totals.</action>
  <verify>A representative fixture renders without missing core game sections.</verify>
  <done>The renderer can produce a coherent paper-style chart document from synced game data.</done>
</task>

<task type="code">
  <name>Expose a PDF download route and portal action</name>
  <files>web/app/api/charting/games/[id]/export-pdf/route.ts, web/app/charting/games/[id]/page.tsx</files>
  <action>Add a PDF response path and a matching download action on the portal game detail page.</action>
  <verify>The portal can download a PDF for a synced game.</verify>
  <done>Staff can trigger PDF export directly from the existing chart detail surface.</done>
</task>

## Verification
- Render the PDF for the sample chart fixture and visually confirm that the header, pitch log structure, and pitcher totals are present.

## Success Criteria
- [ ] Staff can download a paper-style PDF from the portal detail view
- [ ] PDF output stays aligned with the same synced chart snapshot used by CSV export
