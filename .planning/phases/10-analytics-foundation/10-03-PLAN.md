---
phase: 10-analytics-foundation
plan: "03"
type: execute
wave: 2
depends_on: ["10-01", "10-02"]
files_modified:
  - web/lib/charting/analytics.test.ts
  - web/lib/charting/analytics.ts
autonomous: true
requirements: [SC1, SC2, SC3, SC4, SC5]

must_haves:
  truths:
    - "npm --prefix web test -- --run runs to completion with zero failing tests"
    - "npm --prefix web run build completes without TypeScript errors"
    - "All four public functions are exported from analytics.ts: computeSegmentStats, aggregatePitcherStats, computeHitterStats, aggregateHitterStats"
    - "All four pure helpers are exported: computeSegmentStats_pure, computeHitterStats_pure, computePitcherAggregation, computeHitterAggregation"
    - "No test uses it.todo() — every test has a real expect() assertion"
    - "computeSegmentStats_pure returns strikePct=80.0%, whiffPct=37.5%, zonePct=69.2%, chasePct=75.0%, fpsPct=60.0%, kPct=50.0%, bbPct=25.0% against fixtureAnalyticsSnapshot.pitchesForSegA"
    - "Aggregation correctness verified: combining two segments produces weighted (not averaged) percentages"
  artifacts:
    - path: "web/lib/charting/analytics.ts"
      provides: "Complete analytics engine — 4 public async functions + 4 pure exported helpers + TypeScript interfaces"
    - path: "web/lib/charting/analytics.test.ts"
      provides: "Full passing test suite — 20+ test cases"
    - path: "web/lib/charting/analytics-fixtures.ts"
      provides: "Rich fixture used by all tests"
  key_links:
    - from: "web/lib/charting/analytics.ts"
      to: "web/lib/charting/types.ts"
      via: "all exported interfaces reference PitchType from types.ts"
      pattern: "import.*from.*./types"
    - from: "web/app/charting/games/[id]/page.tsx"
      to: "web/lib/charting/analytics.ts"
      via: "NOT yet — Phase 11 owns that migration. calculateStats() is NOT deleted in this phase."
      pattern: "DO NOT TOUCH"
---

<objective>
Verify the complete analytics engine: full test suite green, TypeScript build clean, and all public function contracts confirmed correct.

Purpose: This is the phase gate plan. Phase 11 and Phase 12 can only consume analytics.ts safely once every stat formula is verified against a known fixture and the build is clean.

Output: Green test suite, clean build, confirmation that all five phase success criteria are met.
</objective>

<execution_context>
@/Users/traftonobrien/.claude/get-shit-done/workflows/execute-plan.md
@/Users/traftonobrien/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/10-analytics-foundation/10-CONTEXT.md
@.planning/phases/10-analytics-foundation/10-01-SUMMARY.md
@.planning/phases/10-analytics-foundation/10-02-SUMMARY.md
@web/lib/charting/analytics.ts
@web/lib/charting/analytics.test.ts
@web/lib/charting/analytics-fixtures.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Audit test coverage and fill any gaps</name>
  <files>web/lib/charting/analytics.test.ts</files>
  <action>
Run the full analytics test suite and review results:
```bash
npm --prefix web test -- --run lib/charting/analytics
```

For each failing test:
1. Determine if it is failing due to a bug in analytics.ts OR due to an incorrect expected value in the test.
2. Fix bugs in analytics.ts (never relax a test to match wrong output).
3. If a test has a legitimately wrong expected value (e.g., arithmetic error in fixture design), fix the test assertion — but document the correction with a comment.

**Specific checks to perform:**

a. **Null guard completeness** — Grep `analytics.ts` for every rate stat computation. Verify each uses `pct()` or equivalent null-returning guard. If any uses direct division without guard, wrap it.

b. **KL inclusion** — Verify K% counts both "K" and "KL":
```bash
grep -n '"KL"' web/lib/charting/analytics.ts
```
If missing, add it.

c. **Open PA exclusion** — Verify K%/BB% filter `resultCode !== null` before dividing. If not, fix.

d. **Chase denominator** — Verify Chase% uses `locatedPitches.filter(p => p.locationCell > 9).length` as denominator (not total pitches). If not, fix.

e. **Aggregation sum-not-average** — The computePitcherAggregation test uses two segments with different pitch totals. Verify the combined strikePct is the weighted average (sum all strikes / sum all pitches), not the mean of the two segment strikePcts. If the test is failing because the implementation averages percentages, fix the implementation.

f. **pitchMixPct** — Verify the sum of all pitchMixPct values is approximately 100.0 (within rounding). Add a test if missing.

After all fixes, run:
```bash
npm --prefix web test -- --run lib/charting/analytics
```
All tests must pass.
  </action>
  <verify>
    <automated>npm --prefix /Users/traftonobrien/Desktop/pitch-tracker/web test -- --run lib/charting/analytics 2>&1 | tail -15</automated>
  </verify>
  <done>Zero test failures in analytics.test.ts; every describe block reports all tests passing</done>
</task>

<task type="auto">
  <name>Task 2: TypeScript build gate and public API export audit</name>
  <files>web/lib/charting/analytics.ts</files>
  <action>
Run the production TypeScript build:
```bash
npm --prefix web run build 2>&1 | head -60
```

Fix any TypeScript errors in analytics.ts before proceeding. Common issues to look for:
- `db` import type mismatch (should be `import { db } from "@/db"`)
- Drizzle `inArray` called with empty array — add guard: `if (ids.length === 0) return ...` before any `inArray()` call
- Return type annotations missing on exported functions — add explicit `: Promise<SegmentStats | null>` etc.

**Empty array guard (critical):** Drizzle's `inArray()` throws or returns all rows when given an empty array in some driver versions. Add guards before every `inArray` call:
```typescript
if (paIds.length === 0) return null; // before inArray(chartingPitches.paId, paIds)
if (segIds.length === 0) return null; // before inArray(chartingPlateAppearances.segmentId, segIds)
```

**Export audit** — Verify all required exports are present:
```bash
grep "^export" web/lib/charting/analytics.ts
```
Required exports:
- `export interface SegmentStats`
- `export interface AggregatedPitcherStats`
- `export interface AggregateOptions`
- `export interface HitterStats`
- `export interface PitchGroupStats`
- `export interface AggregatedHitterStats`
- `export function computeSegmentStats_pure`
- `export function computeSegmentStats`
- `export function computePitcherAggregation`
- `export function aggregatePitcherStats`
- `export function computeHitterStats_pure`
- `export function computeHitterStats`
- `export function computeHitterAggregation`
- `export function aggregateHitterStats`

If any are missing, add them. If the build still has errors from other files (not analytics.ts), note them but do not fix — this plan only owns analytics.ts.

After fixes, confirm:
```bash
npm --prefix web test -- --run && npm --prefix web run build 2>&1 | grep -E "error|Error|✓ Compiled|Route"
```
  </action>
  <verify>
    <automated>npm --prefix /Users/traftonobrien/Desktop/pitch-tracker/web run build 2>&1 | grep -c "error TS" || echo "0 TypeScript errors"</automated>
  </verify>
  <done>npm run build completes without TypeScript errors in analytics.ts; all 14 required exports are present; full test suite green</done>
</task>

</tasks>

<verification>
Phase gate checks:
1. `npm --prefix web test -- --run lib/charting/analytics` — 20+ tests, 0 failures
2. `npm --prefix web test -- --run` — full suite green (no regressions in other test files)
3. `npm --prefix web run build` — no TypeScript errors
4. `grep "^export" web/lib/charting/analytics.ts | wc -l` — 14+ exports
5. Cross-check fixture arithmetic: strikePct(80.0), whiffPct(37.5), kPct(50.0) for SegA

**DO NOT delete or modify calculateStats() in web/app/charting/games/[id]/page.tsx — Phase 11 owns that migration.**
</verification>

<success_criteria>
- Full analytics.test.ts test suite: 0 failures
- Full vitest suite: 0 regressions
- TypeScript build: clean (0 errors in analytics.ts)
- All 14 named exports present
- Phase 10 success criteria SC1-SC5 are all satisfied as verified by the test suite
</success_criteria>

<output>
After completion, create `.planning/phases/10-analytics-foundation/10-03-SUMMARY.md`
</output>
