---
phase: 10-analytics-foundation
plan: "00"
type: execute
wave: 0
depends_on: []
files_modified:
  - web/lib/charting/analytics-fixtures.ts
  - web/lib/charting/analytics.test.ts
autonomous: true
requirements: [SC1, SC2, SC3, SC4, SC5]

must_haves:
  truths:
    - "analytics.test.ts exists and imports from ./analytics-pitcher and ./analytics-hitter"
    - "All test stubs fail (red) because analytics-pitcher.ts and analytics-hitter.ts do not exist yet"
    - "analytics-fixtures.ts exports fixtureAnalyticsSnapshot with at least 15 pitches per segment covering all result types"
    - "The fixture has out-of-zone swings, first-pitch strikes, first-pitch balls, and 4 closed PA outcomes (K, KL, BB, 1B)"
    - "npm --prefix web test -- --run lib/charting/analytics runs and reports failures (not config errors)"
  artifacts:
    - path: "web/lib/charting/analytics-fixtures.ts"
      provides: "Rich fixture data for all analytics unit tests"
      exports: ["fixtureAnalyticsSnapshot"]
    - path: "web/lib/charting/analytics.test.ts"
      provides: "Failing test stubs covering all four public analytics functions"
      min_lines: 80
  key_links:
    - from: "web/lib/charting/analytics.test.ts"
      to: "web/lib/charting/analytics-pitcher.ts"
      via: "import { computeSegmentStats_pure, computePitcherAggregation, ... }"
      pattern: "from.*./analytics-pitcher"
    - from: "web/lib/charting/analytics.test.ts"
      to: "web/lib/charting/analytics-hitter.ts"
      via: "import { computeHitterStats_pure, computeHitterAggregation, ... }"
      pattern: "from.*./analytics-hitter"
    - from: "web/lib/charting/analytics.test.ts"
      to: "web/lib/charting/analytics-fixtures.ts"
      via: "import { fixtureAnalyticsSnapshot }"
      pattern: "from.*./analytics-fixtures"
---

<objective>
Create the test scaffold and rich fixture data that all analytics implementation plans depend on.

Purpose: Establish the RED state before implementation begins. Every test must fail because analytics-pitcher.ts and analytics-hitter.ts do not exist yet. Fixtures must be rich enough to verify all stat formulas with known expected values.

Output: analytics-fixtures.ts with deterministic fixture data, analytics.test.ts with all test stubs written as failing tests.
</objective>

<execution_context>
@/Users/traftonobrien/.claude/get-shit-done/workflows/execute-plan.md
@/Users/traftonobrien/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/10-analytics-foundation/10-CONTEXT.md
@.planning/phases/10-analytics-foundation/10-RESEARCH.md
@web/lib/charting/types.ts
@web/lib/charting/fixtures.ts
@web/vitest.config.mts
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Create analytics-fixtures.ts with rich stat fixture data</name>
  <files>web/lib/charting/analytics-fixtures.ts</files>
  <action>
Create `web/lib/charting/analytics-fixtures.ts` exporting a single `fixtureAnalyticsSnapshot` object. This fixture must be large enough that every stat formula is verifiable with known arithmetic.

**Fixture design (two segments in one game):**

Segment A (playerId: "DJames1", segmentId: "fix-seg-A"):
- 15 pitches across 5 PAs: PA-A1 (K), PA-A2 (KL), PA-A3 (BB), PA-A4 (1B), PA-A5 (open — resultCode null)
- Pitch distribution (calculate expected values from these):
  - Pitches 1-15 with pitchResult spread across: called_strike x4, swinging_strike x3, foul x2, bunt_foul x1, in_play x2, ball x3
  - locationCell distribution: cells 1-9 (in-zone) x9, cells 11-14 (out-of-zone) x4, null x2
  - Out-of-zone pitches (cell >9): 3 have swing results (swinging_strike, foul, foul), 1 has ball
  - First pitches (ballsBefore=0, strikesBefore=0): 5 (one per PA), 3 result in strike outcomes, 2 in balls
- Expected values for Segment A (pre-calculate and leave as a comment in the file):
  - totalPitches: 15
  - Strike%: (4+3+2+1+2)/15 = 12/15 = 80.0%
  - Whiff%: 3/(3+2+2+1) = 3/8 = 37.5%  [swings = swinging_strike + in_play + foul + bunt_foul]
  - Zone%: 9/13 = 69.2%  [denominator = 13 located pitches]
  - Chase%: 3/4 = 75.0%  [3 out-of-zone swings / 4 out-of-zone located pitches]
  - FPS%: 3/5 = 60.0%  [3 of 5 first pitches resulted in strikes]
  - K%: 2/4 = 50.0%  [2 strikeouts (K+KL) / 4 closed PAs — PA-A5 open excluded]
  - BB%: 1/4 = 25.0%
  - pitchMix: Fastball x8, Slider x4, Curveball x2, Changeup x1

Segment B (playerId: "CBurrows1", segmentId: "fix-seg-B"):
- 6 pitches, 2 closed PAs (BB, 1B), 1 open PA — minimal segment to test edge cases
- All pitches have locationCell null (tests zone denominator guard)

**Structure the export as:**
```typescript
export interface AnalyticsFixtureSnapshot {
  gameId: string;
  gameDate: string;
  segments: ChartingPitcherSegment[];
  pitches: ChartingPitch[];
  plateAppearances: ChartingPlateAppearance[];
  // Pre-grouped for convenience in tests:
  pitchesForSegA: ChartingPitch[];
  pasForSegA: ChartingPlateAppearance[];
  pitchesForSegB: ChartingPitch[];
  pasForSegB: ChartingPlateAppearance[];
  // Hitter-specific slice for Smith (appears in both segments)
  pitchesForSmith: ChartingPitch[];
  pasForSmith: ChartingPlateAppearance[];
}
export const fixtureAnalyticsSnapshot: AnalyticsFixtureSnapshot;
```

Use hitterName "Smith" for PA-A1, "Jones" for PA-A2, "Smith" again for PA-A4 (Smith has 2 PAs spanning the fixture — use this for hitter aggregation tests).

Import from `./types` only. No db imports. No runtime logic — pure data literals.

The fixture IDs must be stable strings (e.g., "fix-pitch-A-01") so test assertions can reference them by position.
  </action>
  <verify>
    <automated>cd /Users/traftonobrien/Desktop/pitch-tracker && npx tsc --noEmit --project web/tsconfig.json 2>&1 | grep analytics-fixtures || echo "no type errors in fixtures"</automated>
  </verify>
  <done>analytics-fixtures.ts exists, exports fixtureAnalyticsSnapshot with 21+ pitches total, includes pre-grouped slices, no TypeScript errors</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Create analytics.test.ts with failing stubs (RED state)</name>
  <files>web/lib/charting/analytics.test.ts</files>
  <action>
Create `web/lib/charting/analytics.test.ts`. Write all test stubs as real assertions — they must fail because analytics-pitcher.ts and analytics-hitter.ts do not exist yet. Do NOT write placeholder `it.todo()` calls; write actual `expect()` calls that will turn green once the implementation ships.

Import the pure helper functions from the two split files:
```typescript
import { describe, it, expect } from "vitest";
import {
  computeSegmentStats_pure,
  computePitcherAggregation,
} from "./analytics-pitcher";
import {
  computeHitterStats_pure,
  computeHitterAggregation,
} from "./analytics-hitter";
import { fixtureAnalyticsSnapshot as fx } from "./analytics-fixtures";
```

**Test blocks to write (each with full expect() assertions using known fixture values):**

`describe("computeSegmentStats_pure", () => {`
- "returns null for empty pitch array" — `computeSegmentStats_pure([], [])` is null
- "returns null for empty PA array" — `computeSegmentStats_pure(fx.pitchesForSegA, [])` is null
- "computes Strike% from known fixture" — strikePct toBeCloseTo(80.0, 1)
- "computes Whiff% from known fixture" — whiffPct toBeCloseTo(37.5, 1)
- "computes Zone% from known fixture" — zonePct toBeCloseTo(69.2, 1)
- "computes Chase% from known fixture" — chasePct toBeCloseTo(75.0, 1)
- "computes FPS% from known fixture" — fpsPct toBeCloseTo(60.0, 1)
- "computes K% using both K and KL result codes" — kPct toBeCloseTo(50.0, 1)
- "computes BB% from known fixture" — bbPct toBeCloseTo(25.0, 1)
- "excludes open PAs from K% and BB% denominators" — run with PA-A5 included, K% still 50.0
- "returns null (not NaN) for whiffPct when zero swings" — fixture with only ball/called_strike pitches
- "returns null (not NaN) for zonePct when all locationCells are null" — fx.pitchesForSegB returns null zonePct
- "returns null (not NaN) for chasePct when no out-of-zone located pitches"
- "reports correct pitchMix counts" — Fastball: 8, Slider: 4
`})`

`describe("computeHitterStats_pure", () => {`
- "returns null for empty pitch array"
- "computes Chase% for a hitter from fixture pitches/PAs"
- "computes Contact% correctly" — (in_play+foul+bunt_foul)/(in_play+foul+bunt_foul+swinging_strike)
- "computes Whiff% for hitter from fixture"
- "computes K% for hitter — includes KL"
- "computes BB% for hitter"
- "builds zoneFrequency map with counts per non-null cell"
- "groups pitch-type splits into Fastball/Breaking/Offspeed buckets"
- "excludes open PAs from K%/BB% denominators"
`})`

`describe("computePitcherAggregation", () => {`
- "returns null when no segments provided"
- "aggregates two segments — sums numerators before dividing (not average of percentages)"
- "returns correct sessions count"
`})`

`describe("computeHitterAggregation", () => {`
- "returns null when no data provided"
- "aggregates hitter stats across two PA sets"
`})`

These tests reference the pure computation helpers (not the async DB wrappers) — this is the testability pattern from RESEARCH.md.
  </action>
  <verify>
    <automated>npm --prefix /Users/traftonobrien/Desktop/pitch-tracker/web test -- --run lib/charting/analytics 2>&1 | tail -20</automated>
  </verify>
  <done>analytics.test.ts exists with 20+ test cases, running npm test shows "Cannot find module './analytics-pitcher'" or similar import failure confirming RED state — NOT a config error</done>
</task>

</tasks>

<verification>
After both tasks:
1. `npm --prefix web test -- --run lib/charting/analytics` — should fail with import/module error (not config error)
2. `cat web/lib/charting/analytics-fixtures.ts | grep fixtureAnalyticsSnapshot` — exports found
3. `cat web/lib/charting/analytics.test.ts | grep "analytics-pitcher\|analytics-hitter"` — split import references found
</verification>

<success_criteria>
- analytics-fixtures.ts exists with 21+ pitches, all result types represented, pre-grouped slices
- analytics.test.ts exists with 20+ test cases using expect() (not it.todo)
- analytics.test.ts imports from ./analytics-pitcher and ./analytics-hitter (not ./analytics)
- Running the test suite shows failures due to missing module (not config or syntax errors)
- No TypeScript errors in fixtures file itself
</success_criteria>

<output>
After completion, create `.planning/phases/10-analytics-foundation/10-00-SUMMARY.md`
</output>
